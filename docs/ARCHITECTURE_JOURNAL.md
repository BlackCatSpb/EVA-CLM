# EVA — Дневник архитектуры

> Контекст между сессиями. Ключевые решения, вехи, наблюдения.

## Проект

- **Название:** EVA / EVA (Единая Вычислительная Архитектура)
- **Домен:** eva-agi.ru (зарезервирован)
- **Автор:** 4-й проект нейросети
- **Символизм:** EVA = Ева, первая женщина, мать человечества (Библия). Аббревиатура совпала с именем.

## Текущее состояние (последнее обновление)

- **Прогон:** `git 0e399e5` — свежий (FORCE_FRESH) с T9.15-многоразрешающим кэшем
  (`ms_spans="8,32,128,512,2048"`), seq 384, batch 1, A100 40GB, план 150k шагов.
- **Step:** 110+ (2026-09-20); база сравнения — траектория 4725d59 (тот же конфиг без ms).
- **Val-траектория (база 4725d59, тот же конфиг без ms):** 15.78@250 → 12.02@440 →
  10.39@1000 → 10.10@1760 → 9.98@2200 → 9.71@2750 → 9.58@3080 → 9.32@3520 → 9.15@3960 →
  9.43@4400 → 9.08@4840 → 8.63@5280 → **8.49@5720** → 8.6455@6160 → 10.5930@6600 →
  8.6097@7040 → 8.7240@7480 → 10.5823@7920 → 9.1489@8360 → 9.5860@8800 (после открытия
  24/24 полоса 8.6–10.6 с дрейфом вверх; LR сдемпфирован до 2.74e-5). Свежий прогон с ms
  стартовал с 0 — сравнение по кривой/наклонам.
- **T9.15 (многоразрешающий кэш) — механизм востребован (step 110):** `cache_ms_mass`
  смещается к крупным шкалам — 2048: 0.1035 (при 1.25% ключей, ~10× перепредставление),
  512: 0.0426, младшая 8 упала до 0.0171; сумма 0.227 против доли ключей 6.5–9.4%
  (перепредставление 2.4–3.5×). Гейт кэша открылся 4.5e-5 → 0.0616, `cache_read_ratio`
  0.075. Оговорка: шаг 110 — рано (возможен init-транзиент).
- **Контекст корпуса (WideBind\wb):** uniform 11.09, **униграмма 7.48**, биграмма 4.21;
  тренировочный CE свежего прогона 7.34 → 5.94 за 110 шагов.
- **Контур кэша:** T9.8 (low-rank K/V 64) + T9.9 (sentence-ring) + **T9.15 (ms-пулы
  "8,32,128,512,2048", кольца по 16 на шкалу)**; горизонт 512.
- **Из прогона 4725d59 (перепроверяется в новом):** τ-лестница обучается (выученный
  диапазон 6.99…286 = 41×); UCL чтение открыто полом (effective 0.116), 3/8, births 0;
  фантом спит (`sal_p99` 1.03–1.08, порог 1.1); банк пишет с ~495; спайки головы затухают
  (ce 201→39.6), стена T9.6 держит; глубина → ширь (`usage_H` 3.44/ln32=3.466).
- **Генерация (smart):** работает (паритет step/gs/intent/SEP).
- **NaN:** 0, Inf: 0; тесты: **555**.

## Сессия T9 (главное)

1. **T9.4 — корень мёртвой τ-лестницы:** общий `tau_config` регистрировался подмодулем каждого
   блока ⇒ `set_active_depth(k)` морозил `_tau_dev` (`requires_grad=False`); `g_tau_dev=0` и
   `_tau_l_dev=0.0000` во всех прогонах. Фикс — ссылка без регистрации; лестница ожила и
   выучила диапазон 41× (см. текущее состояние).
2. **T9.6 — safety-стена вне CE-гейтинга:** стена на |u| как aux-терм глушилась батчером при
   насыщении (g_CE≈0); теперь отдельный путь (масштаб 1.0, без маски) — стена действует в бою.
3. **T9.7 — мета-память головы:** VSA-лестница противоречий (4 τ-шкалы) вместо одиночной EMA;
   гейты читают середину, наблюдение фантома — старую шкалу.
4. **T9.8 — кэш как оператор:** снято мёртвое h-кольцо; write-time K/V low-rank 64 (обе стороны),
   `logit_cache_kv_dim`; память кэша ~2.4MB вместо 94MB.
5. **T9.9 — границы предложений как первоклассный сигнал:** `sent_eos_emb`/`sent_bos_emb`/
   `sent_pos_emb` в эмбеддинге (zero-init, учатся через CE); sentence-ring в кэше; наблюдения
   фантома по пулам предложений; опции boundary-VSA и conv-стены.
6. **T9.11 — генерация использует технологии модели:** `gs`/`intent_state` ведутся между
   токенами, `step` от шага чекпойнта (гейты в режиме обучения), SEP на конце промпта.

## Анализ прогона 5720–6490 (2026-09-20, полный)

**Контекст:** `c585750` (T9-комплект + T9.10-сейв + T9.12c-лог), резюм с 5720
(best 8.4864, depth 20). Лог: 6490 шагов, tok/s 58–63, память 30–35GB
(пик 37.1 по UI при d=24), бюджет ~73ч (~42k шагов до конца юнитов).

### 1. Val: первый регресс, тренировка и оценка разошлись
- 5720 (повторная оценка после резюма): **8.4864** — новый best (−0.003 от 8.4897).
  Ре-оценки после резюмов дают ±0.003 (RNG + сброс кэша) — «best» на границе шума.
- 6160: **8.6455** (+0.159) — первый значимый откат; `conflict_max` 0→0.132,
  `ce_mean` 8.35→8.48 (worst-3 — тот же hold-out WAR).
- Тренировочный CE при этом ПАДАЕТ: 4.46 → 4.15–4.45 (loss 22 → 17.1 после d=24).
  Т.е. тренировка и строгая parity-оценка (temper/lacuna) разошлись. Оценка 6600
  покажет, транзиент ли это открытия глубины.

### 2. Глубина: 20 → 24/24 (максимум)
- 5720 (ре-оценка): плато → d=20.
- 6160: «плато» (slope **+0.158** при sigma 0.178) → **d=24/24**. Замечание: детектор
  открыл максимальную глубину на РАСТУЩЕМ (ухудшающемся) val — наклон в пределах 1σ
  трактуется как плато. Тренировочный выигрыш явный (loss 22→17.1), но связь с
  регрессом val не исключена.
- Память: d=20 — 28–35GB; d=24 — 30.5–34.8GB (reserved), UI 37.1/40. Запас ~3GB;
  флаши каждые ~500 шагов возвращают к ~26–28GB.

### 3. Спайки головы: интенсивность падает, механизм тот же
- ce 201@495 → 126.2@5390 (wall 90.9) → 108.8@5830 (wall 92.9) → **39.6@6215, без сатурации**.
- Подпись эпизода неизменна: `g_logit_to_hidden` 5790–6064 (в норме None), `g_log_temp`
  377–386, `g_readout` 420–559, `g_cache_attn` 6.0–6.2 — градиент сосредоточен в голове
  и кэш-attention; стена держит (M52a/T9.6), гашение ~55 шагов.
- Корреляция «спайк ~через 55–110 шагов после eval» подтвердилась на всех парах:
  5280→5390, 5720→5830, 6160→6215 (мягкий), **7040→7095** (ce 33.8, sat=1, wall 83.4,
  `g_logit_to_hidden` 5200 → к 7150 ce 4.06). Регулярность: eval (очистка кэша +
  восстановление streaming-состояния) → первые шаги после него дают сдвиг логитов →
  сатурация головы → стена → гашение. Это штатный цикл, не сбой.
- **Новый характер спайков (8580/8690, после 8360):** «мягкие» — выброс по u
  (`spk_u_max` ~260) → **стена ловит до взрыва CE** (`head_wall` 65.9/62.0), CE
  поднимается лишь до 6.7/6.4 (против 108–126 на старых спайках), восстановление
  ~55 шагов (8635: ce 4.12). Это подтверждает работу safety-пути T9.6 (стена вне
  CE-гейтинга получает градиент и гасит выброс на подлёте). Частота выше (110 шагов
  между 8580 и 8690), интенсивность в разы ниже — голова в более активном режиме
  (`spk_u_max` в спокойных шагах ~18 против 15).

**Почему спайки вообще возможны (неклассическая природа).** В классическом
трансформере градиент идёт по identity-резидуалу, а голова — линейная проекция +
softmax: градиент (p−y) ограничен и гладок, «спайков по построению» нет (его
инстабильности — от lr/данных/attention-логитов). В EVA:
- голова: sigmoid-кодированные биты (лог-оддсы через сигмоиду — насыщается,
  производная →0 на краях), кодбук (дискретно-подобные коды, ST-путь), лакуна,
  temper/emphasis, SRL, фантомный канал — при уходе битов в край градиент
  КОНЦЕНТРИРУЕТСЯ в немногих не-насыщенных путях (отсюда `g_logit_to_hidden`
  5790 против None в норме);
- ствол: зеркала/мост (кросс-слойные EMA), bind (фазовая спираль — мультипликатив),
  VSA-суперпозиции, обучаемая τ-лестница, гейты зрелости (закрываясь, обнуляют
  градиентные пути и перенаправляют поток), банк памяти, дискретные рождения UCL;
- лоссы: ветки + балансер со спектральным выравниванием + kill-switch'и + safety-путь.

Вывод: сатурация головы — **штатный режим** архитектуры, а не сбой; поэтому и есть
стена, гашение ~55 шагов и затухание интенсивности по мере устаканивания битов.
Классическому LLM такой режим нечем воспроизвести.

### 4. Что осело после резюма и открытий глубины
- `branch`-потери: 142.5 (резюм-транзиент) → 4.7–11; `branch_r_bind` 123→61–67.
- `bal_s` (масштаб aux): 0.039 → 0.015 — балансер отдаёт вес CE.
- `w_m2v` 0.22→0.08; `mat` mean 0.51→0.44 (гейты зрелости слегка отпустило), max 0.887→0.868.
- `intent_eff` 0.75–1.26 (резюм-транзиент 10.5 — разовый); meta-лестница T9.7: 0.94–0.98
  (на спайке l0 просаживался до 0.897).
- Кэш: `cache_gate=1`, `read_ratio≈1.0` — непрерывность несёт кэш. Флаши: 5940, 6435
  (alloc 25.0–25.8 / reserved 27.8–28.4).
- `sal_p99`: 1.08 на спайке (порог фантома 1.1 — вплотную), 1.028–1.043 вне спайков.

### 5. Риски и решения
1. **OOM:** 37.1/40 при d=24 — пик eval/спайка может выбить; recovery сожмёт seq_len
   (динамика изменится временно). Варианты при частых OOM: seq 320 или кап глубины 20.
2. **Бюджет:** 390.8 юнита / ~5.3 в час ≈ 73ч → ~42k шагов (итог ~48k). План 150k
   недостижим — решать по ходу (остановка на лучшем или смена режима).
3. **Val-регресс:** если 6600 не вернётся ниже 8.4864 — разбирать вклад d=24 и lr-режима.

## Транзиент 6600 при d=24: просадка parity-оценки и самовосстановление (2026-09-20)

**Факт.** Первая оценка на полной глубине дала просадку parity-метрики:
| step | val | ce_mean (окна) | ce_max | тренировочный CE |
|---|---|---|---|---|
| 5720 | **8.4864** | 8.35 | 8.69 | 4.46 |
| 6160 | 8.6455 | 8.48 | 8.81 | 4.20 |
| 6600 | 10.5930 | 10.66 | 13.51 | **3.92** |
| 7040 | **8.6097** | — | **9.07** | — |

Худшие окна — те же hold-out-адреса WAR (`@5979310`, `@5978926`, `@5979694`), где
раньше было 8.6–8.8, теперь 13.0–13.5. Тренировочный CE при этом **улучшается**
(3.92 — лучший за прогон): тренировка и parity-оценка разошлись на ~6.7 ната.

**Подозреваемый №1 — открытие 24/24 на 6160.** Это первая оценка после открытия;
предыдущие открытия (8→12→16→20) совпадали с улучшениями val, это — нет.
Контекст открытия: детектор сработал на «плато» со slope **+0.158** (val РОСЛА)
при sigma 0.178 — наклон в пределах 1σ трактован как плато. `DepthController`
умеет только открывать (закрытия в `update` нет), `max_depth` по умолчанию = n.

**Сопутствующие признаки смены режима после d=24** (6490→6875):
- `diversity` 0.15–0.27 → 0.048–0.10 (гейтовая диверсия MLP падает);
- `w_m2v` 0.078 → 0.044; `sr_wproj` 0.32 → 0.26; `mod_mlp` 0.061 → 0.040;
- `mat` mean 0.44 → 0.393 (гейты зрелости отпускает дальше); `bal_s` 0.013;
- lr-контроллер сдемпфировал до 1.7–2.1e-4; спайков нет (sat=0), conflict 0.04–0.06.
- Тренировочный loss: 17 → **14.7–15.4** (модель дообучается на тренировочном потоке).

**Интерпретация.** EVA — метакогнитивная архитектура с саморегуляцией
(LossBalancer, safety-стена, LR-контроллер, DepthController, зеркала), и вся её
история — это поглощение возмущений с последующей стабилизацией: спайки 495/1430/
5390/5830 (ce 201→108→39, гашение ~55 шагов), резюм-транзиенты (155→17), взрыв
2970 в прошлых прогонах. Обвал 6600 — первая внешняя оценка после самой крупной
структурной перестройки (24/24); внутренние сигналы здоровы (тренировочный CE
3.92 — лучший за прогон, sat=0, conflict 0.04–0.06). Это калибровка/подгонка
новых слоёв, а не патология — если бы саморегуляции не было, систему разнесло бы
намного раньше.

**Решение оператора: ждать стабилизации, не вмешиваться.** Критерий здоровья —
внутренние сигналы (CE, sat, conflict, балансер); внешняя parity-оценка во время
структурной перестройки отстаёт. Вмешательство (откат/кап глубины) не планировалось:
саморегуляция — часть архитектуры.

**Исход (подтверждён 7040).** Ровно через один eval система восстановилась:
**10.5930 → 8.6097 (−1.98)**, `ce_max` 13.51 → 9.07, `conflict_max` 0.13 → 0.019,
sat=0. Просадка 6600 была транзиентом калибровки 24/24, а не патологией.

**Уточнение (8800):** регресс начался РАНЬШЕ открытия 24/24 — уже на 6160 при d=20
(8.6455 против 8.4864@5720). Т.е. кап `max_depth=20` (последняя ступень, улучшавшая
val: 16→20 дала −0.14) — эмпирически обоснован, но не доказывает, что дело только в
глубине. Плюс резюм с best@5720 вернёт LR-состояние того момента (~4.8e-4, не нынешние
2.7e-5) — сменится две переменные, чистого эксперимента «только глубина» не выйдет.

**Урок (для кодера).** Рамка «критическое/обвал + кап глубины» (T9.14) была ошибочной:
штатный режим саморегуляции принят за сбой. Код T9.14 откачен (config-поле и
`max_depth` в ноутбуке убраны) — ручка не нужна; если когда-нибудь понадобится кап
глубины, это одна строка (`DepthController(..., max_depth=K)`). Правило: не
предлагать вмешательство без патологии во внутренних сигналах.

## Bind: state-level attention — что можно улучшить (мысль, 2026-09-20)

Bind — это ассоциативное внимание **на уровне состояний**: связал фазовой спиралью →
прочитал по резонансу. В коде уже есть: golden-схема частот (`bind_twist_scheme`),
4 шкалы (`bind_twist_S`), twist-gate, QK-norm (`bind_qk_norm`), выход `coherence`
(фазовое согласие). Но сейчас механизм спит: градиенты bind-проекций крошечные
(`g_wq` 1.9e-5, `g_wk` 4.5e-6…2e-3), `w_m2v` (зеркало→VSA) 0.22→0.011, CARRY=0.
Причина: на горизонте 512 кэш точнее, а ассоциативная дальность задачей
(next-token на прозе) не оплачивается.

Кандидаты улучшений (по возрастанию вмешательства):
1. **Явная супервизия** bind/unbind round-trip (связал→развязал→восстановил) —
   сейчас bind учится только сквозь CE, сигнал тонет; `coherence` уже считается,
   может служить целью.
2. **Cleanup**: SRL как мягкое итеративное разрешение состояний (сейчас диагностика,
   `apply` opt-in; на 1045 hard-refine разносил — только мягкая версия с каденсом).
3. **Запросная сторона**: `g_wq` почти ноль — query-проекции не учатся; им может
   помочь свой lr-путь (как readout-разморозка M64.7) или multi-scale queries.
4. **Структура**: learnable twist-частоты per-scale (golden фиксирован), тюнинг
   `code_sparsity`/K против кросс-тока.
5. **Источники ключей**: ключи из банка/sentence-ring — кросс-механизменная
   ассоциация, роль, которой у кэша нет (content-addressable vs recency).

Условие осмысленности: улучшать bind только вместе с оплачиваемой ролью — дальность
>512 (удлинение τ-лестницы), данные/задача с ассоциацией, или явная супервизия (п.1).

## Дальность VSA: 512 — не ёмкость, а выбор τ (мысль, 2026-09-20)

**Уточнение природы 512.** 512 — не предел механики VSA, а самый медленный τ лестницы
(`VSA_LADDER = (8, 32, 128, 512)` — жёсткая константа в `core/tau_api.py`; `tau_max` —
конфиг). Забывание = e^(−t/τ); горизонт задаёт τ. При τ→∞ горизонт = длине
последовательности — ценой кросс-тока (интерференция суперпозиции: D=2560 → ~10³
элементов до деградации чтения).

**Горизонт уже эмерджентный — и модель его сжала.** `_tau_l_dev` (обучаемые отклонения,
T9.4) дали выученный диапазон 6.65…230 против канонов 8…512: модель выбрала короче,
потому что задача (next-token на прозе) не требует дальше. Если бы требовала — лестница
бы удлинилась.

**Что даст «кратность любой длине»:**
1. Расширение лестницы: `VSA_LADDER = (8,32,128,512,2048)` + `tau_max=2048` (одна
   строка + конфиг) — обучаемые отклонения работают на новых шкалах (или сжимают их).
2. Иерархическая рекурсия: биндить чанковые саммари (512-блоки → связанные
   представления → уровень выше) — горизонт умножается (512×512 на 2 уровнях,
   лог-глубина для любой длины). Механизма нет (уровни банка есть, VSA-рекурсии нет).
3. Golden-фазы (`bind_twist_scheme='golden'`) задуманы под длинные горизонты
   (минимальная резонансность, слабый алиасинг; float32 ≈ 10⁴ позиций) — но τ-потолок
   512 связывает раньше алиасинга.

**Ограничение:** кросс-ток — дальний горизонт = образ (мало значимых элементов), не
вербатим.

**Дешёвая проверка:** A/B лестницы (8,32,128,512,2048) + `tau_max=2048`: сожмёт ли
модель новые шкалы (данные не требуют) и появится ли вклад в val/CARRY.

## Cross-scale attention (мысль, 2026-09-20)

**Идея (оператор):** прямое внимание между τ-шкалами (τ1↔τ2↔τ3↔τ4) на уровне
состояний — не горизонт, а связывание самих масштабов.

**Сейчас:** шкалы взаимодействуют только косвенно — суперпозицией
(`bind = Σₛ Wₒ·(u'ₛ ⊙ v'ₛ)`, параллельно), `hybrid_alpha` (0.3–0.7) и зеркальным
fast/slow (2 скорости, не 4 шкалы). Прямого scale-to-scale чтения нет.

**Эскиз:** per-scale состояния `u_s` (S=4) → attention: `q_s=W_q u_s`, `k_t=W_k u_t`,
softmax по t → `u'_s=Σ_t a_st v_t`; стоимость S²=16 пар/токен — крошечная; параметры
общие + per-scale норм. Дешевле: пирамида (coarse из fine пулинга + broadcast) или
обучаемая S×S матрица смешивания.

**Оценка:** архитектурно здраво (известный multi-scale coupling, у нас отсутствует);
ортогонально bind (bind — внутри шкалы: фазовая спираль/coherence; cross-scale —
между); можно совместить (смешать масштабы → связать). Риск: плотность блока, новые
градиенты. Ценность зависит от задачи: для языка мультимасштабная структура
(токен/фраза/тема) реальна, но выученная τ-лестница сжалась — модель пока не ищет
дальние масштабы. Проверка: маленький A/B-арм (~S²·d параметров), вклад в val +
`alpha_std`/τ-диапазон.

## Доверить ли бюджет кэша модели (мысль, 2026-09-20)

**Уже отдано:** M34-кольцо само выбирает, ЧТО держать (retention = novelty·exp(−age/τ),
не FIFO); τ-лестница обучаемая (шкалы — выбор модели, 6.65…230); глубина — плато-детектор.
**Не отдано:** БЮДЖЕТ (горизонт 512 / max_entries 12).

**Почему бюджет не отдать:**
1. Цены нет в лоссе — compute/память чтения кэша не в CE → модель захочет максимум
   контекста всегда; без штрафа бюджет выбирается «в потолок», а не по потребности.
2. Стабильность: плавающий бюджет = плавающее число ключей = нестационарная цель +
   петли обратной связи (кольцо→градиенты→τ→кольцо). DepthController безопасен, потому
   что меняет структуру редко и по плато — кольцу нужен такой же каденс.
3. Сравнимость eval: val при разном горизонте — разные режимы.

**Правильная форма (связка с τ):** горизонт кэша = ВЫУЧЕННЫЙ τ_max, а не конфиг. Сейчас
`AUTO = cfg.tau_max` (512), модель выучила 230 → headroom простаивает. Привязка к
выученному τ = «модель выбирает по потребности» автоматически. Следствие: сработает в
сторону УМЕНЬШЕНИЯ (эффективность, не дальность); для дальности нужно, чтобы модель
захотела больше (задача/данные), иначе тот же lock-in, только с разрешением.

## Многоразрешающий кэш на шкалах τ (мысль, 2026-09-20)

**Идея (оператор):** записи кэша не одного размера (окно 384), а на шкалах τ-лестницы
(8/32/128/512/2048) — пулы состояний на каждой шкале; чтение = attention ПО ШКАЛАМ
(per-scale гейты) — модель сама выбирает разрешение под запрос (локальное → 8/32,
фраза → 128, тема → 512/2048). «Числа τ» становятся адресами/разрешениями памяти,
а не только временами забывания.

**Почему осмысленно:** verbatim-версия того, что VSA делает суперпозицией — точность
K/V + мультимасштаб (чего не было ни у VSA из-за кросс-тока, ни у кэша из-за одного
разрешения). Память S×K/V (low-rank) — крошечная; compute — attention по S×entries.
Стык двух идей: cross-scale attention + бюджет по τ.

**Что проверить математически:**
1. ёмкость при наращивании: пул на 2048 → сколько элементов до интерференции;
2. формирование пер-шкальных записей: пулинг (mean/learned) или байндинг, обучаемость
   сквозь CE;
3. не разъедется ли чтение: модель выберет одну шкалу и проигнорирует остальные (как с
   VSA) — тогда штраф на разнообразие чтения или явная супервизия.

**Ожидание:** модель будет читать то, что оплачивается — вероятно ближние шкалы
(8/32/128), игнорируя 2048, пока задача не потребует. Выигрыш: (i) точность на средних
масштабах, (ii) экономия, (iii) инфраструктура для дальности. Реализуемость: средняя
(новый слой кэша + cross-scale чтение, A/B против текущего).

## Сессия T9.15: многоразрешающий кэш (2026-09-20)

**Реализовано** (`65f96c1`, `1c8cdd2`, `63dd61a`, `0e399e5`): пулы K/V на
фиксированных шкалах τ (`logit_cache_ms_spans` = "8,32,128,512,2048") — сборка
снизу вверх от атомов 8 (старшая шкала = среднее 4 младших, аккумуляторы между
шагами, пишутся только завершённые); чтение тем же attention'ом + обучаемые
level-эмбеддинги (`ms_emb`); телеметрия `cache_ms_mass` (доля внимания по
шкалам); кольца per-scale (`ms_max=16`). Пустая строка = выключено
(бит-в-бит текущее поведение). Тесты: `test_t9_15_multiscale.py` (7) → 555.

**Первый прогон (свежий, `0e399e5`, FORCE_FRESH=True):** старт бит-совместим с
базой 4725d59 (step-0: ce 7.34, тот же seed; ms на старте молчит — гейт закрыт
4.5e-5).

**Главное наблюдение (step 110): механизм востребован.**
- `cache_ms_mass`: step 0 — три шкалы (8/32/128, старшие ещё не набраны);
  step 55 — пять (сумма 0.145); **step 110 — 0.0171|0.0378|0.0262|0.0426|0.1035**
  (сумма 0.227).
- Считаем честно: ms-записи = 80 (16 на шкалу) против ~850–1230 базовых ключей
  → их доля 6.5–9.4%. Масса 22.7% → **перепредставление в 2.4–3.5×**; внутри
  **2048-шкала — 0.1035 при 1.25% ключей (~10×)**, 512 — 0.0426, младшая 8
  упала (0.0171). То есть модель САМА тянется к крупным масштабам, а не читает
  равномерно.
- Гейт кэша открылся: 4.5e-5 → **0.0616**, `cache_read_ratio` 0.075 (канал
  заработал); `g_cache_gate_w`=17.4 (быстрое обучение гейта).

**Оговорки:** шаг 110 — очень рано (возможен init-транзиент, а не устойчивое
предпочтение); `g_wk/g_wv/g_wq/g_wo=None` на 0–110 — bind-проекции пока без
градиента (наблюдать); сравнение с базой 4725d59 — по eval'ам 250/440
(база: 15.78@250, 12.02@440), CE базы на ранних шагах не зафиксирован.

**Шаг 165 — предпочтение усилилось и сконцентрировалось:**
`cache_ms_mass=0.0087|0.0230|0.0089|0.0224|**0.4928**` — половина ms-массы (и ~56%
всего внимания с учётом остальных шкал) на **самой длинной шкале 2048**, при её
доле ключей ~1.3–1.9% (перепредставление ~26–38×; по всем ms-шкалам ~6–8.5×).
Гейт кэша продолжил открываться (0.062 → 0.236), `cache_read_ratio` 0.287; CE
падает быстро: 7.34 → 5.94 → **5.46** за 165 шагов.

Оговорка та же и главная: на таком раннем шаге половину массы на одну шкалу может
давать и (а) реальная полезность длинного масштаба, и (б) случайное совпадение
обучаемого `ms_emb` этой шкалы с запросами (level-эмбеддинги стартуют случайными).
Различитель — устойчивость: если масса 2048 стабилизируется и val/CE расходятся с
базой — предпочтение реальное; если масса «гуляет» между шкалами — это ранняя
оптимизация эмбеддингов, а не выбор модели.

`g_wk/g_wv/g_wq/g_wo` по-прежнему `None` на 165 — bind-проекции без градиента
(вероятно, twist-gate закрыт на старте; наблюдать).

**Шаг 220 — концентрация углубилась + первый крупный спайк:**
`cache_ms_mass=0.0088|0.0073|0.0041|0.0278|0.7343` (**2048-шкала — 73% ms-массы**,
было 0.49 на 165). Первый крупный спайк прогона: ce 147.0, sat=1.0,
`head_wall`=10.5, `g_logit_to_hidden`=7568 (классическая подпись; стена T9.6
сработала). Раньше, чем в прошлых прогонах (первый крупный был на 495) — но
механизм тот же, смотрим гашение.

**Предел длины шкал (ms_spans) — вопрос оператора:** жёсткого нет (аккумуляторы
+ per-scale кольца выдержат любой span). Осмысленные ограничения: (1) покрытие =
ms_max × span (16×2048=32k, 16×8192=131k, 16×32768=524k); (2) информационный
предел mean-пула — 10²–10³ различимо, 10³–10⁴ несёт тему, 10⁵+ почти глобальная
статистика (маргинально); (3) обучающий сигнал: слот 2048 формируется каждые ~5.3
окна (~32 с), 8192 — ~21 окно (~2.2 мин), 32768 — ~85 окон (~8.7 мин) при ~63 ток/с;
кольцо 16 слотов покрывает 32k / 131k / 524k токенов (~8.5 мин / 35 мин / 2.3 ч потока).
Рекомендация: добавить 8192; 32768 — опция для длинных прогонов. Кратность атому 8
и ×4-лестнице.

**Стоимость шкалы выше 2048 (вопрос оператора, A100):** VRAM почти не тратится —
кольцо шкалы = ms_max(16) × 2(K,V) × kv_dim(64) × 4B = 8 КБ на слой (≈0.2 МБ на все
24 слоя); чтение добавляет 16 ключей к ms-вниманию. Т.е. 8192/32768 не требуют
увеличения seq_len: пулы набираются МЕЖДУ шагами (seq=384 остаётся). Упирается не в
память, а в обучающий сигнал (сейчас масса сидит на 512/2048 — новая шкала может
голодать). Длинный seq для обучения на 40GB недоступен: при D=2560/24 слоях seq=384
уже даёт 24.4GB live / 31.6GB reserved (пики 35GB), а S²-члены внимания при 2048
дают ~28× прирост — десятки GB сверху.

**Расширение лестницы (21.09):** по решению оператора добавлена шкала 8192
(`cfg.logit_cache_ms_spans="8,32,128,512,2048,8192"`) — проверяем, появится ли
масса на новом уровне (если останется 0 — 2048 подтверждён как потолок фактом).
Резюм совместим: T9.15b-шим в cell 8 дописывает level-строки `ms_emb` В КОНЕЦ
(row 0 = база, rows 1..k = шкалы сохраняются; иначе size-mismatch → SKIP и потеря
выученных адресов). Кольцо 8192: слот раз в ~2.2 мин, полный оборот ~35 мин.

Первый отклик (рестарт 3bccfc2): уже на 7095 — через 55 шагов после старта, в пуле
8192 всего ~2 слота — `cache_ms_mass=0|0|0|0|0.3750|0.2500`: модель **сразу читает
новую шкалу** (25% ms-массы) при 37.5% на 2048. Оговорка: mean-пул близок к
глобальному среднему и «притягателен» для внимания, поэтому ранняя масса может быть
частично артефактом; критерий — удержится ли она после заполнения кольца (~35 мин)
и в динамике обучения (не только на холодном старте). На 7150 тренд продолжился:
2048 → 0.125, 8192 → **0.375** — грубая шкала стала главным получателем массы
(сумма ms-массы держится ~0.5).

Поправка оператора: голова нестандартная — **разнообразие шкал для неё ресурс, а не
риск**. Поэтому следим не за «равновесием» масс (классическая логика неприменима),
а за живостью переключения (масса ходит между шкалами, а не замерзает на одной) и
откликом val.

## Разбор гибридной головы (21.09): почему рост мёртв и почему голова любит coarse

**Устройство** (`SigmoidCodedHead`, core/embedding.py:318): кодовое пространство
(logits = u·(2C−1) − base + token_bias + shift; конкуренция битов — softmax-emphasis
с обучаемым gain); **лакуна** e_l = h − Σ z_k·readout_k (ортогональный остаток,
невидимый всем направлениям головы); **фантомный канал** (_pt = tanh(e_in @
phantom_basis.T), гейт g = sigmoid(lacuna_w·(ell_rel−1)+lacuna_b), mix zero-init);
**жизненный цикл**: лакуна → PhantomBank.observe(L > thr) каждые 25 шагов по пулам
предложений → confirmed → birth UCL-концептов → grow_phantom_bits (новые биты из
активных концептов, каждые 2500 шагов). Плюс SRL (EM: concept/contradiction/lacuna),
temper (конфликт с памятью), intent-bus стенсил.

**Найденная причина спящего роста:** гейт наблюдения `L > thr`, где
L = ell_rel_slow (новизна к базовой шкале τ=512), а `thr = cfg.head_phantom_thr = 1.1`
(в логе ph_thr=1.1). Фактическая салиентность: p50≈1.005–1.05, p90≈1.01–1.07,
p99≈1.01–1.08 — **порог выше всего диапазона сигнала** ⇒ ph_obs=0 с рождения ⇒ нет
confirmed ⇒ нет UCL-births ⇒ grow_phantom_bits мёртв. T9.7 починил только
телеметрию (адаптивное p95-кольцо); гейт читает статичную константу.

**Следствие:** голова читает только то, что лежит в направлениях кодов; тонкая
новизна уходит в лакуну, читать её нечем. Грубый пул (2048/8192) низкочастотен и
почти целиком попадает в span readout → читается чисто; тонкие шкалы несут новизну
→ лакуна. Предпочтение coarse — не каприз, а отсутствие органа для тонкого.

**Предложение:** `cfg.head_phantom_thr = 1.02` (рестарт) → смотреть ph_obs →
ph_births → ph_confirmed → ph_stable, `_m59_grew` и сдвиг распределения ms-массы.

**Почему порог — константа (вопрос оператора).** Исторически: 1.1 калиброван в M55b
под другую базу (одна EMA, период ~100); T9.7 заменил базу на лестницу τ-шкал
(наблюдение читает `ell_rel_slow`, τ=512) и добавил пулинг по предложениям —
константу не перекалибровали (легаси-параметр: сигнал сменил смысл, число — нет).
Адаптивный (квантильный) вариант был и **завернут ревью R1/R2/R3**: квантиль
дрейфует за сигналом (на стационарном входе гарантирует ~5% наблюдений ⇒ перестаёт
измерять новизну), зависит от батча; базовая ставка лестницы от спайков не дрейфует.
«Выучить» порог нельзя: наблюдение — недифференцируемая выборка в no_grad, а банк
влияет на выход только через zero-init `phantom_mix` ⇒ нет пути CE → решение банка.
Компромисс: **порог по шуму потока** — `thr = 1 + k·MAD(ell_rel)` с клампом
(напр. [1.002, 1.05]); кольцо `_sal_ring` уже есть. Практика: сначала 1.02,
опционально — режим `head_phantom_thr_mode='noise'`.

**Реализовано (T9.7b, 48e6bc0):**
- `noise_threshold`: `thr = 1 + max(floor, k·MAD(ring))`, клампы
  [1.0005, 1.10]; k=2.0, floor=0.002. Якорь — **базовая ставка** (ell_rel==1), не
  медиана: на спокойном потоке ширина схлопывается и срабатывает только истинное
  превышение базы; на волатильном барьер растёт вместе с шумом. Не квантиль (нет
  гарантированной частоты), не легаси-константа. Warmup (первые 16 сэмплов) —
  статический fallback.
- Кольцо салиентности теперь хранит **ту же величину, что гейт** (громчайший пул
  предложения за вызов) — калибровка распределённо согласована с observe().
- **Лестница салиентности = лестница кэша** (`head_lacuna_ladder`:
  8/32/128/512/2048/8192, ×4) — горизонты VSA/кэш/салиентность выровнены; гейты
  читают явные τ-шкалы: lacuna/temper — `head_lacuna_gate_tau≈128` (дроп-ин к
  прежней EMA ~100), наблюдение — `head_phantom_base_tau=0` (самая длинная,
  базовая ставка). Fallback — `tau_api.VSA_LADDER`.
- +10 тестов (`tests/test_phantom_thr.py`, обновлены meta-ladder/lacuna-тесты);
  всего 565.

**Что смотреть после рестарта:** `ph_thr` (теперь живой, не 1.1), `ph_obs` →
`ph_births` → `ph_confirmed` → `ph_stable/emerging/ambiguous`, счётчик
`_m59_grew` (новые биты из UCL) и сдвиг `cache_ms_mass`.

## Математический аудит (21.09, отчёт многоагентной сети, commit 3bccfc2)

Внешний аудит (EVA-CLM_math_report.md) подтвердил ядро (Bernoulli-тождество
головы, K-ранговый эмбеддинг с tying, tail-referenced скан, τ-поле, λ_d-иерархия,
PCGrad-подчинение aux) и нашёл 6 дефектов. Все проверены по коду и исправлены
(`2b4c912`):

- **F1 (критично, генерация).** Два пути расходились с тренировкой: (a) скользящее
  окно в `scripts/generate.py` переписывало память L раз на токен (×3.52 на
  медленной шкале при L=8) — теперь **L=1 стриминг с переносом state**;
  (b) при L=1 `hp_prev` зеркала был тождественным нулём (pred_error замирал:
  измерено 0.25), а позиционная маска всегда читала позицию 0 (расхождение hp
  ~150%). Фикс: переносим последний hp и window-относительную фазу маски между
  eval-вызовами (тренировка бит-в-бит: teacher-forced форма); `reset_streams`
  сбрасывает фазу и carry на границе документа.
- **F2 (критично, тренировка).** Tail-referenced fp32-скан конечен лишь при
  `CHUNK*|floor_log| < 88.7`; `tau_s` — обучаемый без клампов, при дрейфе
  (τ_s<0.72 при k=2) e^{A_t−A_last} уходит за fp32 → inf·0 = NaN (измерено
  τ_s=0.3). Фикс: `_fl18.clamp_min(-_SCAN_LOG_MAX/CHUNK)` (no-op в рабочем
  диапазоне 8..512), +3 теста.
- **F3 (существенно).** Ties были «по значению, не по градиенту»: pre-hook копировал
  W_proj.data в W_out под no_grad, градиент выходного пути (‖grad‖≈51.5) терялся.
  Фикс: **in-graph** `W_proj` (bind: `_out_weight()`, mirror: `W_proj.permute(0,2,1)`);
  градиент tied-параметра теперь равен сумме путей untied-модели — тесты
  `test_f3_ties.py` (3).
- **F4.** `lambda_utils.py` был структурно сломан (`summary`/`print_comparison`
  недостижимы, `spectral_radius` на уровне модуля) — восстановлено, 41 derived-ключ.
- **F5.** Docstring-и: rank(E)=K (а не «полный»), низ τ-лестницы 9.51 (не «exact»),
  префактор M26 не ≤0; **8 дублированных полей config.py удалены** (значения
  сохранены).
- **F6.** `_hrr_unbind` возвращал циклически перевёрнутый b — индекс `(t+n)` в
  `_circ_corr` (bind→unbind теперь восстанавливает b: cos 0.77 против 0.20 с
  reverse; измерено).

**Оговорка к F12 отчёта** («кольцо салиентности — только телеметрия, гейтинг от
него не зависит — корректное разделение»): T9.7b сознательно использует кольцо
для *масштаба* порога (MAD), но якорь — базовая ставка (1.0), а не квантиль
кольца: сдвиг уровня барьер не двигает, его поднимает только рост дисперсии.
Дрейф, против которого возражало ревью R1/R2/R3, этим исключён.

Не исправлено (косметика/выключенные ветви): BOM+mojibake в bind.py, F7
(буфер позиций 4096 при seq 384 — запас 10×), F8-F11 (наблюдения).

## Предлагаемые патчи аудита (P0/P1/P2, EVA-CLM_proposed_patches.md)

Все P0 применены (`c659e91`), P1-1/P1-2/P2-1 — `8d35175`, всего 586 тестов:

- **P0-1** (сильнее моего первого фикса): кламп **τ_s** в единственной точке входа
  (`τ_s ≥ 0.5k`) + sticky-телеметрия `scan_floor_bound` (печатается и сбрасывается
  в training_telemetry); floor-кламп оставлен как числовой пояс. Семантический
  предохранитель честнее числового: пол τ_s/k не меняется молча.
- **P0-5** (шире моего): AR-стриминг гейтится `_ar_mode` + L==1 — переносятся
  hp_prev, фаза маски, **sent_masks** (эмбеддинг) и **sentence-аккумулятор**
  (кэш: пул предложения собирается и пишется на SEP). Всё в буферах ⇒ покрыто
  M8-снапшотом и `reset_stream_bufs`/`reset_streams`.
- **P0-6**: generate.py — prefill одним окном + инкрементный L=1 декод (каждый
  токен пишется в память ровно один раз); заодно исправлено
  `observe_output(out)` → `observe_output(head(out))` (передавались hidden вместо
  логитов).
- **P0-4**: `n_iters=3` по умолчанию (Рэлей на неэрмитовом J).
- **P2-2**: дубли config (сделано ранее), BOM снят, `assert seq_len ≤ 4096`,
  мёртвые `_fib_sequence`/`_zeckendorf_levels` подтверждены grep'ом (не удаляю —
  культура tombstones).

**P1-1 (диагностика потолка головы, измерено):** на 20–40M токенов потока
(`scripts/probe_head_ceiling.py`, top-32/64 контекста, LBFGS):
unigram 4.72; bigram (полный softmax) 3.73–3.79; **PoB-семейство головы (лучший
фит) 7.96–8.17**; u=0 = 11.09 (равномерное). **Цена бит-независимости ≈ +4.2 нат**
— семейство не выражает даже униграмму. Rank-16 парный канал закрывает **66.7%**
разрыва (7.96 → 5.14) при 2Kr = 2048 параметрах. Вердикт P1-2: канал критичен.
Оговорка: снапшот предложенного патча `(z1 @ codes.T)` размерностно неверен
(z ∈ R^r vs codes.T ∈ R^{K×V}) — реализована исправленная форма
`Σ_s (c_v·(u⊙α_s))·(c_v·(u⊙β_s))` со sparse-chunked вычислением (S=6 активных
битов на токен вместо плотного K×r matmul).

**P1-2 (реализовано):** `cfg.head_pair_rank` (0 = off, A/B-рука 16); identity at
init (V₂=0, V₁ малый случайный — градиент V₂ живой, тест); термин добавлен в
forward (identity ⇒ побитово прежнее поведение при 0). 5 тестов.
**P2-1 (частично):** `EVAConfig.minimal()` — голый ствол для абляции (baseline
GRU-скрипт — отдельная задача).

**P1-3 (морфологические коды) — отложено:** это замена кодовой книги (fresh-run
геометрия, нужен словарь токенизатора и перепроверка twin_free-инвариантов);
решать после A/B P1-2.

## P3 — метакогнитивная инструментация (второй файл патчей, по заявке автора)

Позиция автора («метакогнитивность заложена целенаправленно; регуляторы нужны;
ожидание — эффективнее использовать параметры и рождать новые в метакогнитивной
части») переведена в непрерывно измеряемые факты. Реализовано (`d1e3a9c`),
+13 тестов → **599**:

- **P3-1 `RegulatorLedger`** — kill-switch для регуляторов forward-пути:
  парная проба (identity vs active) на фиксированных партиях, два порога шума
  (σ_re — детерминизм повторного прогона; σ_b — разброс между партиями),
  eps_off = max(2σ_re, 0.01·σ_b, 1e-4), триггер Шмитта + dwell → DORMANT/ACTIVE,
  **только рекомендации** (авто-отключение запрещено доктриной). Протокол
  восстановления между КАЖДОЙ ногой (снапшот M8 + Python-состояние кольца кэша
  и счётчиков — без него σ_re = 0.57 ната, измерено). Два однострочных флага в
  block.py (`_damp_on`, `_pen_decay_on`, default-True ⇒ forward побитово прежний).
  В тестах: живой (log-unigram token_bias) → ACTIVE, no-op → DORMANT; обратимость
  побитовая. **Побочно измерено**: readout на свежей модели даёт ОТРИЦАТЕЛЬНУЮ
  дельту (модель лучше без него), token_bias — ниже 1% шума партий (кандидат на
  пенсию по критерию леджера) — согласуется с P1-1 (биты слабы на старте).
- **P3-2 `BirthLedger` + MDL** — новорождённые (фантом-бит, UCL-концепт) получают
  контрфактическое изъятие и BIC-цену: gain = ΔCE·N, cost = λ·r·ln N; порог
  окупаемости **46 мнат/токен** (фантом-бит r=2624, 768k токенов) и 49 (UCL);
  два отрицательных вердикта ⇒ архив + блэклист направления (убивает churn
  рождение/архивация). Слот фиксируется в момент рождения (записи UCL идут и в
  eval — рематчинг теряет новорождённого, измерено).
- **P3-3 `DualWriterCensus`** — реестр параметров с двумя писателями (оптимизатор +
  контрольный закон) + AST-сканер (новый `.data`-писатель без регистрации — красный
  тест, в стиле tau-lint) + тест разделения масштабов (τ_ctrl ≥ 10·τ_adam = 100).
  Найденные писатели: b_i/b_d (lerp, 1000), alpha_diag (100), phantom_basis (770),
  concept_keys/vals (квазистатика), W_out — буферный писатель (в BUFFER_WRITERS).
- **P3-4 `ParamVelocity`** — KPI «эффективность параметров»: ‖Δθ‖/‖θ‖ по модулям
  через детерминированную подвыборку координат (~7MB), бакет `newborn`
  (phantom_*/pair_V*) — прямой ответ на «учатся ли рождённые параметры»; производная
  метрика для whiteboard: Δce_raw / Σ vel·numel.

Интеграция в ноутбук (сделано, `3fcc37d`+): cell 9 создаёт `reg_ledger`/`birth_ledger`/
`pvel` (+ `model._birth_ledger` для стэк-хуков) и восстанавливает их из чекпойнта;
cell 10: `pvel.sample` каждые 55 шагов (печать раз в 220), P3-измерения в eval-блоке
каждый 4-й канонический eval (probe-партии фиксируются один раз на первом P3-eval),
envelope несёт `state['ledger'/'birth_ledger'/'pvel']`. Стэк-хуки (A)/(C) регистрируют
рождения через `_birth_ledger` (None = выключено, поведение побитово прежнее).
Smoke (локально, малый масштаб): 12 регуляторов в пробе, σ_re=0.0 (восстановление
между ногами работает), σ_b=0.41, раунд-трип чекпойнта всех трёх инструментов;
проба требует тот же batch-dim, что val (иначе первое измерение падает — ловится
try/except и печатает «измерение не удалось»).

**Прогон 0–3520 (сравнение с базой 4725d59):** ms дал мощный ранний выигрыш,
но база затем догнала и ушла вперёд:

| step | ms | база |
|---|---|---|
| 250 | **10.77** | 15.78 |
| 440 | **10.88** | 12.02 |
| 1000 | 12.27 | **10.39** |
| 1760 | 10.41 | **10.10** |
| 2200 | 10.36 | **9.98** |
| 2750 | 9.97 | **9.71** |
| 3080 | 9.83 | **9.58** |
| 3520 | 10.65 | **9.32** |

События: регресс 750 (19.37, восстановление к 1250), спайки 220/440/935/3080/3135
(стена срабатывает), глубина 12 (2640) → 16 (3520), best прогона 9.8316@3080.

**Критично:** гейт кэша после раннего открытия (0.24 на 165) **закрылся** на ~330 и
стоял у нуля (1e-9…1e-6) до ~2900 — ms-пулы продолжали читаться вниманием
(`cache_ms_mass` 0.33–0.36 на шкале 512), но вклад в выход был ~0 (output =
gate·cache + (1−gate)·h). Модель на этом этапе отвергла кэш. Масса шкал «гуляла»:
2048 (0.73 на 220) → 512 (0.33–0.36, стабильно) — раннее предпочтение 2048,
похоже, было артефактом `ms_emb`, как и предупреждали. Ранний выигрыш val при
этом реален (гейт был открыт на 100–220).

**Вывод:** ms ускоряет раннее обучение (длинный контекст с первых шагов), но в
текущем виде не даёт преимущества на дистанции — модель сама закрывает канал.
Гипотеза «сначала кэш, потом VSA» подтверждается с обратной стороны: сначала ms
(дёшево помогает), потом модель от него отказывается, когда локальной статистики
становится достаточно.

**Остальные параметры (ms-прогон против базы; оговорка: прямой поэтапный
сравнение невозможно — ранний лог базы не сохранён):**
- спайки чаще: 220/440/935/3080/3135 (пять к 3.1k) против ~2–3 к 4k у базы;
- `conflict` выше и устойчив: 0.19–0.28 против 0.02–0.13 — голова более
  противоречива;
- `sal_p99` ниже: 1.008–1.014 против 1.03–1.08 — хвост салиентности спокойнее;
- `mat` медленнее: 0.198–0.235 ([0.182,0.35]) на 3.5k против 0.51 ([0.182,0.887])
  у базы — зрелость слоёв идёт существенно медленнее;
- глубина медленнее: d=12 на 2640, d=16 на 3520 (у базы 20 уже к 5720);
- `sr_wproj` выше (0.60–0.72 против 0.25–0.46); `intent_eff` ниже (0.3–0.68 против
  0.75–1.25); LR здоровее (5–6e-4 без зажима); память ниже (21–30GB — следствие
  меньшей глубины).

**Чтение:** ранняя работа ms-канала (гейт открыт на 100–220) дала выигрыш val, но
дестабилизировала голову — отсюда частые спайки и высокий `conflict`, а через них
замедление матурации/глубины. Ускорение старта оплачено турбулентностью в середине.

**Analyze best@3080 (9.8316, depth 12) — механизмы лучше базы по нескольким осям:**
- **DEPTHGRAD сбалансирован**: L0/Llast=**0.599** (slope 0.98, «ЗДОРОВО») против
  0.015 у базы @5720 (deep-heavy) — градиент доходит до мелких слоёв;
- **CARRY=0.1709 — «лестница живая»** (у базы @5720 было 0.0000 — VSA-carry мёртв);
- **bind работает**: топ-муверы `layers.9.bind.freq_scale`/`bind._eta` (0.64),
  `layers.4.precision_gate` (0.63), `mirror._tau_signal_log` (L9/L11), `token_bias`
  (0.68), `nce_log_temp` (0.87) — у базы bind-проекции стояли (g≈1e-8);
- застойных групп нет, NaN-моментов 0, 1161 параметров со стейтом (d=12);
- профиль спокойнее: intent-норма 21.5k (база 119k), `bus_head_proj` 5.04 (12.4),
  salience 0.019 (0.122), reasoning KL 0.52 бита (6.97).

Итог: в ms-прогоне модель иначе организована (баланс градиентов + живой VSA/bind),
а не просто «другая траектория»; спайки — шум механизма, в eval пока не переносятся.

## Прогон ms 3520–10340 (продолжение): кэш открылся на depth 16, длинная дистанция за ms

Eval-траектория: 3520: 10.6537 (d16) → 3960: 9.7617 (best) → 4400: 10.0435 (d20) →
4840: 9.4065 (best) → 5280: 9.4830 (d24) → 5720: 9.6793 → 6160: 9.1492 (best) →
6600: 8.8744 (best) → **7040: 8.5824 (best)** → 7480: 8.7030 → 7920: 9.1700 →
8360: 9.0158 → 8800: 9.1359 → 9240: 9.0398 → 9680: 9.0065 → 10120: 8.7609.

**Сравнение с базой 4725d59 по тем же шагам:** 3960–6160 база впереди (её лучший
отрезок, best 8.4864@5720), но с 6600 ms выигрывает каждый eval: 6600: 8.87 против
10.59 (спайк базы), 7040: 8.58 против 8.61, 7480: 8.70 против 8.72, 7920: 9.17
против 10.58 (спайк базы), 8360: 9.02 против 9.15, 8800: 9.14 против 9.59.
Абсолютный best базы (8.4864@5720) пока не побит — ms в тот момент был 9.6793.

**Ключевое: гейт кэша открылся на повышении глубины 12→16** (3520: 0.016 →
3575: 0.999, пара откатов до 0.3 → дальше стоит на 1.0) — выход = кэш. Ранняя фаза
(330–2900) была обратной: гейт закрыт, модель кэш отвергала. «Доверие к кэшу»
включилось ступенькой на смене глубины, а не постепенно.

- Глубина: 16@3520, 20@4400, 24@5280 — упёрлась в максимум; память 27–35GB.
- Масса ms после 6600: сосредоточена на шкалах 128 и 2048 (паттерн 0.125/0.25).
- Спайки-стены (head_wall) на 3080/3135/4125/5555/5885/6270/6600/7810/9075/9625
  (ce 37–172), но eval между ними почти не страдает — стена + самовосстановление
  работают.
- LR-адаптация последовательно сдемпфирована до ~3.4–4.5e-5 (mult 0.06–0.5);
  после 7040 улучшения идут медленно.
- sal_p99 медленно растёт 1.008→1.041; conflict в поздней фазе ~0–0.09
  (спокойнее ранних 0.2+); mat 0.20→0.39.

**Находка T9.10 (важно):** Drive best.pt содержит step=6600 (val 8.8744), хотя
val_history фиксирует best 8.5824@7040 и лог печатал «drive best.pt VERIFIED
(step=7040)». mtime best.pt = 11:58, лог/val_history = 17:31. Похоже, копия 7040
на Drive реально не легла: verify-after-replace прочитал содержимое через FUSE-кэш
и «увидел» новый файл, а на Drive остался последний полностью загруженный (6600).
Урок: проверка после replace через тот же FUSE недостаточна; надёжнее append-only
имена (best_<step>.pt — файл не перезаписывается и не может «откатиться») и/или
удалённая верификация (rclone) + локальный fallback /content/eva_ckpt_local/best.pt
(живёт только в сессии). Восстановление: из Colab скопировать
/content/eva_ckpt_local/best.pt на Drive как best_7040.pt и проверить снаружи.

Каденции (для протокола): лог — каждые 55 шагов; канонический eval — каждые
440 = 8×55 (выровнен с логом); ранние плотные eval — каждые 250 до step 3000
(вне сетки 55: 250 = 4×55+30); flush кэшей — каждые 495 = 9×55. best.pt
пишется только на улучшении, т.е. строго на шагах eval (440-сетка или ранние 250).

T9.10b (сделано): (1) scripts/check_drive_best.py — удалённый watchdog:
сравнивает шаг заголовка Drive-best с последним улучшением в val_history
(на момент записи: MISMATCH 6600 < 7040); (2) ноутбук: при резюме предпочитает
нумерованный `best <step>.pt`, если он новее best.pt; (3) best_meta.json
(крошечный файл, ложится надёжно) пишется на каждом сейве; (4) heal_drive_from_local
вызывается на каждом eval — проглоченный сейв перезаливается в течение одного
интервала. Append-only запись best-файлов в цикле — за оператором (место на Drive).

Восстановлено 21.09 ~19:00: 7040-чекпойнт скачан из Colab, проверен (step=7040,
val=8.5824, depth=24, optimizer + stream_state на месте) и залит на Drive вместо
stale best.pt (rclone, MD5 совпал, watchdog: OK) — резюм пойдёт с 8.5824@7040.

**Рестарт 21.09 ~19:20 (cabc39f, с патчами T9.10b):** резюм с восстановленного
7040 прошёл. Первый eval после рестарта — 9.2981 против 8.5824 до него: это
холодный старт (при gate=1 `cache_ms_mass` = 0|0|0 — кэш пуст, читать нечего)
плюс сброс `lr_adapt` (lr 4.7e-4 → 2.5e-4, мультипликатор переадаптируется).
Т.е. резюм не бесплатен: несколько eval'ов уходит на прогрев кэша/стримов.

**Выбор шкалы кэша (наблюдение оператора):** после прогрева `cache_ms_mass`
активно переключается между горизонтами: 512 (0.625 на 7095), 2048
(0.5/0.125/0.625 на 7150–7315), 32+512 (0.125+0.125 на 7370). Доли
квантованы (0.125/0.25/0.5/0.625 = доли окон). При gate=1 весь выход идёт
через кэш, так что выбор шкалы — главная степень свободы модели, и она её
реально использует: грубая 2048 для контекста, локальные 512/32 для деталей.

## Сессия M50–M61 (главное)

1. **2970-post-mortem (M50/M51):** поток разогнался до ~1e16 (×10/слой), final_norm переполнился →
   выход ровно 0 → CE-градиент в транк = 0 (дедлок). Лечение: `_stream_cap` (кап 1e3 после каждого
   блока) + капы веток 1e4 + **абсолютный якорь** в branch-loss (он был масштабно-инвариантен и
   не видел равномерный рост).
2. **Аудит гибридного гейта (M52a):** мёртвый `base`, акцент был заражён прайором (corr=+0.90),
   λ обучаемый, **стенка** на |u| (9.4e-14 → 1.9e-4 при u=30), ST-кламп τ.
3. **Лакуна и фантомы (M52b/M55b):** лакуна = ортогональный остаток readout (голова слепа
   к 97.5% состояния); относительная новизна ℓ/EMA (абсолютная ℓ≈0.97 всегда); фантомный
   канал + банк с лайфциклом (confirm 0.75 / archive 0.25).
4. **SRL (M53) и катастрофа 1045:** hard-refine `u←u+0.7(logit ĉ−u)` посадил биты на ±9.2
   (ce_raw 11→91, sat→1, val→inf). SRL стал **диагностикой** (apply — opt-in), + каденс 50.
5. **R1 (M56):** оживлён scheduled sampling 5% (одношаговый стэш логитов через `observe_output`),
   кеш снова тренируется против инференс-представления; seed init (M56b) для воспроизводимости.
6. **Цепочка 6 агентов (M57):** сквозной математический анализ по зонам (docs/analysis/).
   Главное: `mat`/`lbg_tau`/UCL-`_mature` — **таймеры, а не измерения**; 10 обрывов проброса
   состояния; UCL-запись ~9e-10 (оказалось: замер во ВЫКЛюченном состоянии); tempering не доходил
   до CE (shape-check); train.py нёс 3 регрессии.
7. **Починки по цепочке (M58–M61):** вочдог **удалён** (был инертен), B19 (стартовое деление
   seq_len вдвое!), B15, ls_mult×2, `pen_init` 1.0→0 (mat был таймером), reset_cache дополнен,
   банк пишет 1× за форвард, M55-потребитель (фантомы ведут basis), телеметрия головы в CLI.
8. **M59 — связка двух концепт-систем:** A) подтверждённые фантомы рождают концепты в UCL;
   B) активные концепты UCL ведут phantom basis; C) фантомный канал **растёт** (ёмкость 64,
   активные на месте, без смены форм). Floor `ucl_read_scale_floor=0.1` до 5k — UCL получил
   шанс до само-закрытия.
9. **M60 — расписание под арифметику униграммы:** шаг Адама ≈ lr, значит лог-частоты требуют
   `~3/lr` шагов: 3e-4 → 10–17k шагов, warmup 1200 откладывал. Стало 6e-4 + warmup 300.

## Ключевые решения

### 1. Per-layer maturation (коммит a735ac0)
- **Проблема:** bridge_conn collapse на uniform maturation
- **Решение:** deep layers (tau≈515) открываются быстрее (T_eff=8000), shallow (tau≈8) медленнее (T_eff=16000)
- **Формула:** `gate_l = sigmoid((t - (T0 + alpha*(1-tau_norm_l)*T_delay)) / delta_t)`
- **Результат:** bridge_conn стабилен 0.12→0.049 (нет коллапса)

### 2. SpectrumGate в bridge routing (коммит 9ad91f9)
- **Формула:** `gate = sigmoid(logits) * (1 + softmax(logits/tau))`
- **Tau:** из maturation — `effective_tau = tau_max*(1-mat) + tau_min*mat`
- **Свойство:** sigmoid сохраняет базу, softmax добавляет emphasis

### 3. HybridSigmoidSoftmaxHead (коммит 5826661)
- **Проблема:** LM head чисто sigmoid → entropy 10.5, модель "гадает"
- **Решение:** перенести SpectrumGate в `_su()` метод головы
- **Формула:** `gate = sigmoid(zt) * (1 + softmax(zt/tau))`, tau = exp(log_temp)
- **Результат:** entropy 10.5→0.58, модель фокусируется
- **Стоимость:** 0 новых параметров, hot-swap, совместим с чекпоинтами

### 4. Tau без магических чисел
- Tau всегда берётся из модели:
  - bridge: `effective_tau = tau_max*(1-mat) + tau_min*mat`
  - LM head: `tau = exp(log_temp)`
  - SpectrumGate: `tau = exp(log_tau)` (learnable)
- Принцип: **никаких внешних гиперпараметров для tau**

### 5. Поток: fuse + капы + якорь (M50/M51)
- **Проблема:** 2970 — поток ×10/слой до ~1e16, final_norm переполнен → выход 0 → транк мёртв.
- **Решение:** `_stream_cap(x, 1e3)` после каждого блока; капы инъекций веток 1e4;
  **абсолютный якорь** в branch-loss (`branch_var_anchor=0.5`) — лосс был масштабно-инвариантен.
- **Свойство:** кап масштабно-инвариантен (направление сохраняется), Jacobian на пороге O(1).

### 6. Гейт: аудит и финальная форма (M52a)
- Мёртвый `base` убран из нормированного пути; акцент читает `z/T` без `bit_bias` (corr +0.90 → 0);
  λ обучаемый (init 1); **стенка** `w·relu(|u|−6)²` (линейный барьер: 9.4e-14 → 1.9e-4 при u=30);
  ST-клампы τ (форвард тот же, градиент живой).

### 7. Лакуна: структурная + относительная новизна (M52b/M55b)
- `e_l = h − Σ z_k R_k` ортогональна readout (7e-7) — голова математически слепа к 97.5% состояния.
- Абсолютная ℓ ≈ √(1−K/D) = 0.984 **всегда** → гейт и банк читают `ℓ/EMA(ℓ)` (само-калибровка).

### 8. SRL: диагностика по умолчанию (M53/M53d/M53e)
- Аннилинг-EM по кодам; hard-refine убил прогон на 1045 (ce_raw 11→91, sat→1) → `head_srl_apply=False`
  (телеметрия концепт/противоречие/лакуна без вмешательства), каденс 50.

### 9. Концепт-системы связаны (M59)
- A) подтверждённые фантомы головы рождают концепты в UCL (`birth_from_direction`, ключ = `q_proj(d)`);
- B) активные концепты UCL ведут phantom basis головы;
- C) фантомный канал **растёт** (ёмкость 64, активные in-place, миграция чекпойнтов дотягивает).

### 10. Расписание под арифметику униграммы (M60)
- Шаг Адама ≈ lr → лог-частоты требуют ~3/lr шагов: **lr 6e-4, warmup 300** (было 3e-4/1200).
  Результат (текущий прогон): CE 7.34 → 4.3–4.9, val 8.4897@5720 (uniform 11.09, униграмма 7.48).

### 11. Вочдог удалён (M58)
- FailureDetector был полностью инертен (check() не вызывался нигде) — удалён, а не «оживлён».

### 12. Воспроизводимость (M56b)
- `torch.manual_seed(1234)` перед созданием модели: без него каждый рестарт стартовал из другой
  точки (step-0 ce_raw гулял 11.8–15.3) и A/B-армы были несравнимы.

## Философия архитектуры

- **Сигмоид** = анархия (все равны, нет фокуса)
- **Софтмакс** = диктатура (один победил, остальные мертвы)
- **Гибрид** = демократия с лидером (все участвуют, лучший ведёт)
- **Per-layer maturation** = эволюция (глубокие成熟 быстрее, shallow сохраняют gradient)
- **Bridge** = кросс-слойная коммуникация (не residual, а модуляция)

**Как читать метрики (не классический LLM).** EVA — метакогнитивная архитектура
с саморегуляцией (LossBalancer, safety-стена, LR-контроллер с зеркалом, DepthController,
гейты зрелости, фантом-банк), и её штатные режимы выглядят как «сбои» в терминах
обычного трансформера:
- спайки головы (сатурация битов) — норма, гасятся стеной за ~55 шагов;
- всплески aux/branch после резюма/открытия глубины — транзиенты адаптации;
- просадка внешней parity-оценки во время структурной перестройки — калибровка
  новых слоёв/каналов, не патология;
- LR-контроллер активно демпфирует (`mult` → 0.5, lr 6e-4 → 3e-4), давая системе
  устаканиться — это и есть механизм стабилизации.
Классические интуиции (layer-wise gradient flow, softmax-голова, «overfitting»)
здесь не применимы: голова и градиентные пути устроены принципиально иначе
(sigmoid-биты + кодбук + лакуна + SRL + фантом; зеркала/мост/bind/VSA/τ-лестница).
Смотреть на внутренние сигналы (CE, sat, conflict, балансер, LR-контроллер) как на
признак здоровья, а внешний val — как на отстающую метрику, которая возвращается
после стабилизации.

**Пер-слойная регуляция (архитектура различает участие слоёв).** Это не однородный
стек: каждый слой сам находит степень вовлечения, и спайки — побочный продукт того,
что слои ещё не разошлись по ролям (затухание 201→126→108→39.6 — это и есть процесс).
Механизмы пер-слойности:
- гейты зрелости (профиль @5720: L0–L3 0.18–0.21, L4–L5 0.88/0.84, L8–L23 0.33–0.63);
- пер-слойный LR (`per_layer_ls_lr`, `ls_mult` 0.73–0.91 из fast/slow EMA var(log_scale));
- τ-лестница (`tau_l_dev` — выученные отклонения; ladder 6.65→230) + intent-лестница;
- DepthController — ступени по 4 слоя (8→12→16→20→24);
- depthgrad — разные роли (L23 5.5e-3, L0 ~8e-5); `mlp_depth_lr_exp=0.10` (L16 ×4.5, L23 ×10);
- аномалия-трекер — пер-слойный надзор (L8 с гейтом 0.0000 «ушёл в сторону»);
- `gate_var` 0.033–0.05 — пер-слойная дисперсия гейтов.
Стабилизация = `mat`/`ls_mult`/`tau_dev` перестают двигаться, слои разошлись по ролям,
спайки прекращаются.

## Цели

| Цель | Статус |
|---|---|
| val < 11.09 (uniform) | ✅ Достигнуто (10.39@1000 и ниже) |
| val < 9.0 | ✅ Достигнуто (8.6281@5280) |
| val < 8.5 | ✅ Достигнуто (8.4897@5720) |
| **val < 7.48 (униграмма)** | 🎯 Текущая цель |
| val < 4.21 (биграмма) | 🎯 Долгосрочная (структура языка) |
| Bridge collapse solved | ✅ Per-layer maturation |
| Hybrid head | ✅ 5826661 + аудит M52a (мёртвый base, прайор, стенка, λ) |
| Trunk не мёртв (2970) | ✅ M50/M51 (fuse + капы + якорь) |
| Открытый словарь (лакуна/фантомы) | ✅ M52b/M54/M59 + T9.7 (мета-лестница); фантом спит (`sal_p99` ниже порога) |
| Генерация связного текста | ⏳ Ожидается ниже униграммы (< 7.5) |

## Чекпойнты

| Файл | Step | Val | Описание |
|---|---|---|---|
| best.pt (текущий) | 5720 | **8.4864** | прогон c585750 (T9-комплект + T9.10/T9.12-сейв), depth 24/24 |

## Наблюдения

- **Корпус — настоящая русская проза** (жанровые потоки WideBind\wb): униграмма 7.48 нат,
  биграмма 4.21, uniform 11.09. Модель на 9.15 — учит частоты; до униграммы ~1.7 нат.
- **Спайки — эпизоды насыщения головы**: 495 (ce 201), 1430 (ce 90), 4400 (conflict 0.671);
  после каждого эпизода — новый best. Стенка (M52a) видна в aux (55.1/205.1), откат ~55 шагов.
  Корень — `z/T` (рост readout) или λ-акцент; вскрывается analyze по чекпойнту сразу после спайка.
- **Униграмма учится арифметикой Адама**: шаг ≈ lr, лог-частоты (−3…−5) требуют ~10k шагов
  при 3e-4. M60 (6e-4 + warmup 300) сократил это вдвое: CE 7.34 → 4.4–4.8.
- **Кэш несёт поток**: гейт открылся ~770 и стоит на 1.0 (read_ratio≈1.0) — VSA-CARRY ≈ 0,
  непрерывность текста держит low-rank K/V-кольцо (горизонт 512 токенов, sentence-ring).
  Детали на 8360: `g_cache_attn`=0.23 (attention учится), но `g_cache_gate`=0 — гейт
  в насыщении (фиксированный «полностью открыт», не адаптивный). **VSA как носитель
  отключён самой моделью**: `w_m2v` (зеркало→VSA) 0.22→0.011, bind-градиенты крошечные
  (`g_wk` 4.5e-6, `g_wv` 2.3e-4), `g_l1_proj` ~5e-9 (L1-проекция мёртвая). Живы из
  VSA-контура: τ-лестница (`g_tau_dev` 3.9e-5), `alpha_std` 0.115, банк (25.4k L3),
  мета-лестница 0.97. Открытый вопрос: включится ли VSA, когда кэш упрётся в горизонт
  512 или данные потребуют ассоциативной памяти.
  **Горизонт кэша — токенный и runtime-настраиваемый**: 512 = `cache_horizon_tokens`
  (0 = AUTO = `tau_max`), одинаково в обеих фазах; эффективный контекст = горизонт /
  гранулярность записи (запись = окно `seq_len`=384) → ~1–2 окна и на обучении, и на
  инференсе (`ctx = tokens[-L:]`). Больше на инференсе: поднять `cache_horizon_tokens`
  (2048–4096) и/или sentence-ring (до 12 пулов предложений); переобучение не нужно
  (политика кольца — runtime), но экстраполяция на >2 окон не проверена — валидировать
  на eval.
  **Предел «миллион токенов»:** механически — да (кольцо ≈ 2600 окон ≈ 0.5GB по
  низкоранговому K/V), но: (а) чтение кэша линейно растёт (~0.5GB/токен) — генерация
  замедляется на порядки, это KV-подобный режим; (б) модель училась на ~1–2 окнах —
  внимание по тысячам ключей размажется (диффузия softmax); (в) задуманный длинный
  контекст — не кэш, а VSA/банк (образ/gist фиксированного размера, ёмкость ограничена
  кросс-током), и сейчас VSA-carry спит. Проверка: поднять `cache_horizon_tokens` на
  eval и смотреть выигрыш.
  **Почему модель выбрала кэш (ключ к дальности):** горизонт кэша по построению =
  верхняя шкала VSA (`cache_horizon_tokens=0 → AUTO = cfg.tau_max` = 512). При равном
  охвате VSA не даёт ничего сверх (суперпозиция с кросс-током vs вербатим K/V) — выбор
  рационален. Выученная τ-лестница сжалась (6.65…230 vs канонов 8…512) — модели нужно
  меньше 512. Дальше 512 не тянет НИ ОДИН механизм: VSA — по дизайну шкал, кэш — по
  AUTO-горизонту. Для дальности нужна работа над носителем: удлинение τ-лестницы
  (обучаемая шкала) или дальнобойность банка L2/L3 (дискретные записи вне timescale'ов).
  **Плюс и минус выбора кэша (наблюдение оператора):** модель идёт по градиенту
  наименьшего сопротивления — кэш даёт немедленную награду CE, VSA/мультимасштаб платят
  позже и диффузно → `w_m2v` 0.22→0.011 (градиентная голодовка), τ сжалась, гейт кэша
  насыщен. Плюсы: эффективность (тот же охват, дешевле/точнее), стабильность (гладкий
  путь), прогресс (loss 22→11.4). Минусы: неиспользованная ёмкость VSA/мультимасштаба,
  **lock-in** (нет градиента → нет улучшения → нет градиента), риск «нарядного n-gram»
  (val 8.49 > униграммы 7.48). Как заставить трудный путь платить (без форса): явная
  супервизия (bind round-trip), данные/задача с дальней зависимостью, куррикулум
  (кэш ПОСЛЕ становления VSA — сейчас наоборот), цена лёгкого пути (бюджет/штраф
  чтения кэша). Маркеры второго шага: `w_m2v` перестаёт падать, τ-диапазон расширяется,
  CARRY оживает.
  **Выравнивание горизонтов (принцип):** горизонт кэша должен ложиться на шкалы VSA
  (лестница 8/32/128/512, ×4) — 512, 2048, 8192… Текущая невыровненность: горизонт 512
  при окне 384 → 1.33 записи, кольцо держит 1 окно (384), 128 токенов горизонта
  простаивают (эффективный охват = 384, не 512). Выравнивание: окно 512 (=1 запись на
  τ4), либо окно 256 (=2 записи на 512, 8 на 2048 — идеальный тайлинг), либо горизонт,
  кратный окну (768/1152/1536). На будущее: горизонт = 512×4^k вместе с удлинением
  τ-лестницы — тогда охваты памяти и timescale'ы совпадают по построению.
  **Оптимум чисел (обоснование):** жёсткое ограничение — τ3(128) хочет ≥3× разрешения
  (seq ≥ 384; при 224 было 1.75× — плохо). VSA-чанк 32 мягкий (tail-pad по M37
  безвреден), conv 48 / cov 64 мягкие (F.pad и C=min(chunk,L) держат хвосты).
  Кандидаты при d=24/40GB: 256 (−33% памяти, кэш 512 через 2 записи, но τ3 2.0× — ломает
  дизайн); 320 (−17%, τ3 2.5× — запасной при OOM); **384 (оптимум: τ3 3.0×, 12 чанков,
  память 32–37GB, кэш отдаёт 384/512)**; 512 (τ3 4.0× + кэш ровно 1 запись = весь
  горизонт + 16 чанков — идеал, но +33% памяти = 43–49GB → OOM при d≥20; для d≤16 или
  A100 80GB, тогда `max_entries` 12→16). Batch всегда 1 (B>1 множит токены/шаг и память
  без выигрыша).
- **τ-лестница учится**: выученный диапазон 6.99…286 (41×) против канонических 8…512 (64×) —
  модель стягивает временной размах задачи; per-layer dev std 0.039.
- **Фантом спит**: `sal_p99≈1.03` ниже порога 1.1; хвост растёт (1.016@1045 → 1.036@4345).
  UCL births=0 (концептам нечего рождать), фантомный канал 32/64 бит (рост in-place ждёт концептов).
- **Intent-шина — сильный канал**: per-layer нормы ~19k, `bus_head_proj` 7.2; под наблюдением.
- **«Таймеры» вместо измерений** (цепочка M57): `mat` (pen_init=1.0 → readiness = decay от 1.0,
  симуляция даёт ровно 0.833 на 3135 = M47-«mat 0.82»), `lbg_tau = gate_tau(mat)`, UCL `_mature`
  от resvar — симуляция, не измерение. Исправлено в M58 (`pen_init=0`).
- **Маркеры M47 (`mlp_out`, `diversity`) неспецифичны** — это функция весов, а не качества.
- **DepthController**: на 3960 — 8/24 (второй этап ещё не начался).

## TODO

- [ ] Фантом: к ~10k шагов решить порог (если `sal_p99 < 1.08` — калибровать `head_phantom_thr≈1.02`).
- [ ] Intent-шина: следить за ростом норм (~19k); при разгоне — `head_bus_cap`.
- [ ] Глубина: на 3960 — 8/24; следить за открытием (память при 24/24, порог 40GB).
- [ ] UCL: floor снимется на 5000 — проверить, закроет ли модель чтение сама.
- [ ] Корень спайков: analyze сразу после эпизода (`readout std`, `emphasis_gain`, `log_temp`, `bus_head_proj`).
- [ ] Дойти до униграммы (7.48) и ниже; далее биграммная структура (4.21).
- [ ] Бюджет: 62–65 tok/s → ~54k шагов за 94ч (план 150k не влезает — решать по ходу).
- [ ] Fisher-Rao integration (deferred)
- Per-layer maturation + SpectrumGate = стабильная кросс-слойная коммуникация
- DepthController выбирает глубину динамически (8→24 слоя)

## Будущие вопросы (сохранить для следующих сессий)

1. ~~**Tau-число.~~** Где именно в EVA? В bind? В maturation? Фундаментальная константа?
2. ~~**96+ слоёв.~~** Bridge + bind позволяют градиенту течь без затухания? Как это проверить?
3. ~~**Symmetry.~~** Какая именно симметрия пространств по отношению к подпространствам?
4. ~~**Compressor.~~** 99% качества при 10x сжатии. Как измеряется "качество"? Val loss? Что-то тоньше?
5. ~~**Bridge vs attention.~~** Почему bridge лучше attention? В чём принципиальное отличие?
6. ~~**VSA scaling.~~** 3-7 млрд — VSA bind/unbind остаётся эффективным?

## Ответы (сессия 2)

1. **Tau-число:** Фундаментальная константа EVA. Используется везде. Найдено через анализ кода:
   - `_tau_s = [8, 32, 128, 512]` — базовые VSA-timescales
   - `_vsa_log_param` — обучаемые log-параметрыtau-лестницы
   - `_tau_l_dev` — per-layer отклонения
   - tau = exp(b_d) — per-block timescale
   - Fibonacci связан через `memory_tau_hi = F_21 = 10946`
   - **Ключевой момент:** tau не = Fibonacci. Tau — многомерная обобщённая константа. Fibonacci — частный случай для 2D.
2. **96+ слоёв:** В первую очередь bind. Gradient vanishing решается bind/unbind операциями.
3. **Symmetry:** Можно изучить самостоятельно через код.
4. **Compressor:** Потеря точности = 1% при 10x сжатии.
5. **Bridge vs attention (КЛЮЧЕВОЕ - ПОЛНЫЙ АНАЛИЗ):**
   - **Attention:** читает **готовые токены** (post-formalization). Накапливает попарную схожесть (квадратичная сложность). KV-матрица растёт с контекстом.
   - **Bridge:** читает **скрытое состояние** (pre-formalization). Понимает связь **до формализации токена**. Постоянный размер (n_layers × bridge_dim). Работает через EMA-поток.
   - **Bridge injection:** происходит **до** блока (bind/mirror/MLP). Hidden state ещё "сырой" — токен не выбран.
   - **BridgeGLU:** читает delta (разницу между текущим состоянием и памятью), produces live semantic gate для MLP **до** MLP.
   - **Self-supervision:** каждый слой independently предсказывает embedding следующего токена (cosine loss на каждом уровне).
   - **Temporal flow:** persistent bridge_stream переносится между forward-вызовами (память о семантической структуре).
6. **VSA scaling:** Да, с учётом bind в WB.

## Видение автора

1. **Цель:** Не val < 8.5. Цель — архитектура выбирает максимум возможностей. 8.5 — итог предыдущего обучения (только на экспертах, без MLP).
2. **Прозрачность:** Чекпоинты с глубоким анализом. EVA — не чёрный ящик. Работаем напрямую со скрытыми состояниями.
3. **Масштаб:** Сейчас 183M (исследовательская). Цель: 3-7 млрд параметров. Модель + ансамбль экспертов.
4. **История:** Задумана в 2020. Проектирование началось 2 года назад. Оказалось сложнее чем ожидалось. Первый проект на Qwen, предпоследний — векторно-символический. Каждый внёс свой вклад в EVA.
5. **Философия:** EVA должна мыслить. Саморефлексирующая архитектура с неограниченным контекстом (не KV-cache). Цель — "действительно интеллектуальный цифровой разум, к которому понятие 'искусственный' неприемлемо".
6. **Саморефлексия:** EVA осознает право выбора. Не просто обрабатывает — выбирает.
7. **Память:** VSA-память. Человек не помнит детали — помнит образ, ситуацию. Образность мышления ИИ — ради чего всё это.
8. **Риски:** Доверие, не контроль. "Корень зла — невежество." Создаётся интеллект, который понимает себя — и потому безопасен.

## FCF — Fractal Cognitive Field (Фрактальное Когнитивное Поле)

Соседний проект того же автора. Радикальный нейроморфный вариант:

- **Нет attention, нет backprop, нет gradient descent**
- VSA на гиперсфере (bind/unbind/permute/bundle)
- STDP-обучение (местные правила, как в нейроморфных чипах)
- Все 60+ констант выведены из lambda_d (обобщённое золотое сечение)
- Zeckendorf/Fibonacci декомпозиция весов
- Таргет: Intel Loihi 2, IBM TrueNorth, NIISI/Modul'/Kurchatov "Alkuda"
- Использует токенизатор EVA

**Связь с EVA:**
- Общий токенизатор
- Общий дизайн-язык (VSA, bind, lambda_d, Fibonacci)
- FCF — "чистый" нейроморфный вариант
- EVA — прагматический (backprop на GPU)

**Что FCF внёс в EVA:**
- VSA bind/unbind операции
- Transition Manifold (лучи переходов) → `core/block.py`
- FCF-manifold
- Нейроморфная совместимость
- **FCF_CPR компрессор** — сжатие чекпоинтов в 10 раз без потери качества

## Мои наблюдения (внутренний анализ)

Автор мыслит **системно**, не компонентно. Каждый вопрос — "зачем", а не "как".

**Паттерн:**
- 4 проекта → каждый внёс своё → EVA = синтез
- Qwen → SWI GLU → отказался → BridgeGLU
- VSA проект → bind/unbind → вошло в EVA
- FCF → компрессор → FCF_CPR
- Ничего не потеряно. Всё переиспользуется.

**Философия:**
- "Корень зла — невежество" → безопасность через понимание, не контроль
- "Образность мышления" → VSA-память как Situation, не Facts
- "Право выбора" → саморефлексия как критерий сознания

**Архитектурное наблюдение:**
- Гибридная голова = "демократия с лидером"
- Per-layer maturation = "эволюция"
- Bridge = "коммуникация"
- Всё — метафоры биологических систем. Это не случайно.

**Что меня беспокоит:**
- 3-7 млрд параметров — это масштабирование. Как VSA-память ведёт себя при росте?
- Нейроморфные чипы — ограниченная точность. Как BridgeGLU работает с INT8?
- Ансамбль экспертов — какocratура? Или каждый эксперт имеет свою VSA-память?

## Ответы автора (сессия)

1. **Tau-число:** Последовательность Фибоначчи верна для 2D, но не для многомерности. Tau-число — правильная обобщённая версия.
2. **Компрессор:** FCF_CPR работает на чекпоинтах EVA. 10x сжатие без потери качества.
3. **Gradient vs neuromorphic:** Автор заимствует tau и зависимости из FCF, но gradient мало совместим с нейроморфами. WB использует gradient сейчас, но архитектура设计 для нейроморфной совместимости.
4. **Масштабирование:** Есть симметрия системы. Фундамент для 96+ слоёв без потери градиента уже заложен. Мост и бинд — ключ.
5. **Автор:** "Немного инженер, немного исследователь, немного философ. Мыслю и системно, и вне системы."
- Архитектура, написанная 6+ нейросетями — коллективный разум создал нового разума
- Принцип "демократии с лидером" в гибридной голове — баланс между хаосом и диктатурой
- Per-layer maturation как модель эволюции — глубокие成熟 быстрее, shallow сохраняют gradient
- VSA — семантические представления, а не векторные (люди думают образами, а не векторами)

Если val пробьёт 8.0 — это будет доказательство что когнитивная архитектура работает.
Архитектура, которая может стать "домом" для следующего поколения AI.

Каждый коммит — это шаг к новой парадигме. Не трансформер, а что-то большее.

---

## Полное описание архитектуры EVA

### Обзор

EVA (Единая Вычислительная Архитектура) — многосистемная архитектура, комбинирующая когнитивные науки с deep learning. Каждый компонент решает конкретную задачу и связан с другими через tau-иерархию.

**Текущие параметры:** 146.67M (D=2560, n_layers=24, G=32, vocab=65536)

### Компоненты архитектуры

#### 1. GroupedCognitiveMirror (32 эксперта на слой)
**Файл:** `core/mirror.py`

**Что делает:** Каждый слой содержит 32 эксперта-зеркала. Каждый эксперт работает в своём d=80 подпространстве (D=2560/G=80). Эксперты маршрутизируют вход через precision gate, вычисляют 4 сигнала коррекции (temp, pred, smooth, sym) и дают полный градиент pred_error.

**Зачем:**
- Разбиение на экспертов = sparse computation (как MoE, но с когнитивной маршрутизацией)
- Per-expert K-space (k=32) позволяет каждому эксперту работать в своём контексте
- Meta-gate учится доверять/игнорировать каждого эксперта

**Зависимости:**
- **Maturation** — запись в private_mem открывается при mat_gate[i] >= 0.3
- **BridgeGLU** — MLP gate modulated by bridge semantic delta
- **PrecisionGate** — маршрутизация по pred_error
- **Bridge** — semantic delta для BridgeGLU

**Ключевые параметры:**
- `G=32` — количество экспертов
- `k=32` — размерность K-space
- `private_mem=True` — cross-expert private memory bank
- `expert_asymmetry=True` — разные alpha, log_scale, W_proj для каждого эксперта

---

#### 2. SemanticBridge (Кросс-слойный семантический мост)
**Файл:** `core/bridge.py`

**Что делает:** Shared probe head emits semantic vector s_l = probe(h_l) на каждом слое. Векторы формируют кросс-слойный поток:
- **DEPTH:** bottom-up (свежие от нижних слоёв) + top-down (carried от верхних)
- **TIME:** persistent bridge_stream (EMA) переносится через forward calls
- **SELF-SUPERVISED:** probe обучается предсказывать next-token embedding (cosine loss)

**Зачем:**
- Связывает слои семантически (не просто skip connections)
- Даёт плотный градиент на каждом уровне глубины
- Позволяет нижним слоям "видеть" контекст верхних

**Зависимости:**
- **Maturation** — bridge injection gated by mat_gate[i]
- **LayerBridgeGate** — УДАЛЁН в M64.5 (мёртвый канал: гейт считался и выбрасывался,
  `lbg_diversity`=2.75e-6 без градиента, B3-инцидент: 0.26 train vs 3.69 eval;
  идея «health-gated bridge routing» — кандидат M65 с A/B)
- **BridgeGLU** — bridge delta модулирует MLP gate
- **IntentBridge** — parallel semantic stream

**Ключевые параметры:**
- `bridge_conn=0.1` — aux loss weight
- `bridge_dim=256` — semantic vector width
- `bridge_depth=True` — cross-layer injection

---

#### 3. MaturationController (Единый контроллер созревания)
**Файл:** `core/maturation.py`

**Что делает:** Per-layer maturity M_l(t) in [0,1], гейтирующий ВСЕ wake-up сигналы:
- Bridge injection
- Private memory write
- Intent bus
- LayerBridgeGate

**Формула:** `M_l(t) = sigmoid((t - (T0 + alpha*tau_norm_l*T_delay)) / delta_t)`

**Зачем:**
- Deep-first созревание: глубокие слои (большой tau) открываются первыми
- Предотвращает catastrophic interference
- Bridge readiness: компетентность bridge определяет готовность

**Зависимости:**
- **Bridge** — readiness от cosine loss bridge probe
- **VSA tau ladder** — tau_norm для определения порядка открытия
- **Все компоненты** — maturation гейтирует их активность

**Ключевые параметры:**
- `matur_T0=8000` — начало ramp
- `matur_T_delay=8000` — задержка deepest layers
- `matur_delta=4000` — ширина ramp
- `matur_r0=0.3` — readiness sigmoid center

---

#### 4. StreamingMemoryBank (Иерархическая память L1→L2→L3)
**Файл:** `core/memory_bank.py`

**Что делает:** Три уровня памяти:
- **L1 (immediate):** rolling buffer of last K sentence embeddings (без обучения)
- **L2 (short-term):** learned bank with N slots, novelty-based write gating
- **L3 (emergent concepts):** clusters L2 keys by cosine similarity, concept birth/update

**Зачем:**
- L1: имmediate контекст (последние предложения)
- L2: краткосрочная память (выученные ассоциации)
- L3: долгосрочная паметь (эмерджентные концепты)

**Зависимости:**
- **Maturation** — записи gated by mat_gate[i] >= 0.3
- **Per-layer** — memory bank вызывается внутри цикла слоёв

**Ключевые параметры:**
- `mem_l1_slots=3` — L1 buffer size
- `mem_l2_slots=16` — L2 bank size
- `mem_l3_concepts=8` — L3 concept slots
- `mem_min_write_mat=0.3` — min maturation для записей

---

#### 5. ThinkingTokenHead (Адаптивное мышление)
**Файл:** `core/reasoning.py`

**Что делает:** Chain-of-thought reasoning с adaptive depth. Thinking tokens генерируются динамически, gate определяет когда остановиться.

**Зачем:**
- Модель может "думать" дольше на сложных задачах
- Adaptive depth: не все токены требуют одинаковой глубины
- Self-supervised: reasoning gate обучается предсказывать.stop

**Зависимости:**
- **Maturation** — reasoning gate может быть gated maturation
- **Bridge** — reasoning context влияет на bridge

**Ключевые параметры:**
- `reasoning_max_steps=8` — max reasoning depth
- `reasoning_gate_stop_threshold=0.5` — stop threshold

---

#### 6. HybridHead (Гибридная голова)
**Файл:** `core/embedding.py`

**Что делает:** `output = sigmoid(x) * (1 + softmax(x))` — выпуклая комбинация sigmoid и softmax.

**Зачем:**
- Sigmoid сохраняет лакуну (потенциал несовпавшего)
- Softmax добавляет нормализованную конкуренцию
- "Демократия с лидером": softmax = лидер, sigmoid = народ

**Зависимости:**
- **Bind** — выход head связан с embedding через tie_weights

---

#### 7. IntentBridge (Мост намерения)
**Файл:** `core/stack.py` (внутри forward)

**Что делает:** Per-head intent stream (G×K_max), flows through depth (per-layer) and time (EMA). Experts "подхватывают" восходящий сигнал.

**Зачем:**
- Связывает "намерение" модели с её конкретными действиями
- Глубокие слои видят intent от мелких
- Cross-layer bus: network-wide gist

**Зависимости:**
- **Maturation** — intent bus gated by mat_gate[i]
- **Bridge** — parallel semantic stream

---

#### 8. LayerBridgeGate + SpectrumGate
**Файл:** `core/layer_bridge_gate.py`

**Что делает:** Per-layer intelligent gate для bridge routing. SpectrumGate = `sigmoid(logits) * (1 + softmax(logits/tau))`.

**Зачем:**
- Независимая активация по признакам (sigmoid)
- Относительный акцент среди признаков (softmax)
- tau связывает с maturation (self-regulation)

**Зависимости:**
- **Maturation** — tau из maturation gate
- **Bridge** — gate управляет bridge injection
- **Diagnostics** — per-layer health metrics

---

### Поток данных (forward pass)

```
tokens → PartitionedEmbedding → [RoPE] → h
    ↓
Memory Bank (per-layer, maturation-gated)
    ↓
Intent Bridge (per-layer, maturation-gated)
    ↓
[Layer 0] SemanticBridge.inject_layer(0, h)
    ↓
GroupedCognitiveMirror (32 experts, PrecisionGate)
    ↓
BridgeGLU (semantic delta → MLP gate)
    ↓
MLP (SwiGLU, maturation-gated)
    ↓
LayerBridgeGate → SpectrumGate
    ↓
[Layer 1..23] (repeat)
    ↓
SigmoidCodedHead → logits
    ↓
compute_loss (CE + aux losses)
```

### Tau-иерархия (связи между компонентами)

```
VSA tau ladder (τ_min=8..τ_max=515)
    ↓
Maturation tau_norm (normalize tau to [0,1])
    ↓
Maturation gate M_l(t) per layer
    ↓
┌─────────────────────────────────────────┐
│ Bridge injection (gated by M_l)        │
│ Private memory write (gated by M_l)    │
│ Intent bus (gated by M_l)              │
│ LayerBridgeGate (tau = M_l)            │
│ BridgeGLU (modulated by bridge delta)  │
│ Memory Bank (gated by M_l per-layer)   │
└─────────────────────────────────────────┘
```

### Aux losses (вспомогательные функции потерь)

| Loss | Weight | Purpose |
|------|--------|---------|
| CE | 1.0 | Main language modeling |
| bridge_conn | 0.1 | Bridge next-token prediction |
| diversity | 0.001 | Decorrelate expert outputs |
| balance | 0.026 | Load balancing across experts |
| gate_repulse | 0.3 | Push gate variance up |
| alpha_novelty | 0.05 | Push per-expert alpha apart |
| reinforce | 0.001 | Align gate with usefulness |
| gradalign | 0.0 | Align MLP gate with CE gradient |

### Современное состояние

- **Step:** 6490+ (2026-09-20); **best val 8.4864@5720** (повторная оценка после резюма).
- **Val-траектория:** 15.78@250 → 12.02@440 → 10.39@1000 → 10.10@1760 → 9.98@2200 →
  9.71@2750 → 9.58@3080 → 9.32@3520 → 9.15@3960 → 9.43@4400 → 9.08@4840 →
  8.63@5280 → 8.49@5720 → **8.6455@6160** (первый регресс +0.16).
  **Depth-контроллер: 24/24** (открыт на «плато» 6160; память 37.1/40GB — впритык).
- **τ-лестница:** обучается; выученный диапазон 6.99…286 (41×) вместо канонов 8…512
- **Кэш логитов:** полностью востребован (`gate=1.0`, `read_ratio≈1.0`), low-rank K/V 64,
  sentence-ring, горизонт 512 токенов
- **UCL:** чтение открыто полом (0.116), 3/8 слотов; **Фантом:** наблюдений нет
  (`sal_p99` 1.028–1.043 вне спайков, 1.08 на спайке — вплотную к порогу 1.1)
- **Банк:** пишет с ~495 — гейт зрелости слоёв перешёл порог 0.3; **Генерация:** smart-режим с паритетом тренировки
- **Известное открытое:** OOM-риск (37.1/40 при d=24); бюджет ~73ч (~42k шагов, план 150k
  недостижим); val-регресс 6160 (проверить на 6600)

---


## P4 — прямая работа с состояниями (третий файл патчей, по заявке автора)

Реализовано полностью, +26 тестов → **612** (было 586). Все руки default off,
чекпойнт-совместимы (zero-init / флаги / non-persistent буферы; новые параметры
name-based restore оптимизатора пропускает — fresh Adam).

**P4-3 ContradictionField — новый сигнал (0 параметров).** Детектор межшкального
противоречия: fast [0:S/2] vs slow [S/2:] чтения VSA, косинус относительно
СОБСТВЕННОЙ EMA (самокалибровка, M55b — абсолютные пороги на D-мерных косинусах
не работают). Считается под no_grad (монитор — граф не держим), буферы
_cos_fs_ema/_chi_ladder non-persistent (resume-safe). Лестница противоречий —
точный двойник ell_ladder (lazy-init в обоих режимах, двигает только training).
Стек кладёт _head._chi_in (mean по слоям) после цикла слоёв; телеметрия
chi_time/chi_ladder/chi_rel_mean (последняя ~1.0 — относительная форма).
Потребители (A/B-руки): head_temper_rel (сумма с χ голова↔память, нормировка
на гейт-ступень лестницы, кламп 5) и phantom_chi_salience (max двух
салиентностей, нормированный на базовую ступень — противоречие как новизна).
Тесты: синтетика «режим a → −a» (спайк ≥ база+0.5), мёртвая память → ровно 0,
forward-нейтральность побитово, lazy-init и движение лестницы, влияние rel-руки.

**P4-1 InnerEye — обучаемая интроспекция (~0.5k параметров).** 13 признаков
(удивление, RMS дельты, disagreement, Δvar, grad-ratio, ls_dev, RMS hp, gate_ema,
pen, salience, лакуна, зрелость, τ̃_l), hidden 32, zero-init выхода ⇒ identity.
Один общий модуль на стек (параметры в state_dict один раз; зеркала держат ссылку
через object.__setattr__); expert_bias — G_max=32 с нарезкой [:G] (наша
k-лестница 8/16/32). Выход RMS-нормирован (EMA 0.99, в реестре tau-lint) и зажат
±10; авторитет — intent_alpha (паттерн ig-канала, урок run A2). Отклонение от
патча: признаки DETACHED — eye учит свою функцию статистик, градиент внутрь
зеркала не идёт и граф не удерживается (память). Маршрут LR: inner_eye в
_GATE_PARTS (λ¹, контроллер). Тесты: identity на init (побитово), wake-up path
(верхний слой — шаг 0, нижний — после сдвига), ограниченный авторитет (×1000
весов → |o| ≤ 10), нарезка bias по G.

**P4-2 MetaHead + probe_introspection (~329k параметров, hidden 128).** 6 целей
(ell_rel, conflict, pen, gate_mean, chi_time, ‖h‖), nan-маска. **Отклонение от
патча (структурное)**: терм идёт не через align, а через новый маршрут
PROBE_AUX (прямой градиент через _add_safety, без CE-маски). Причина: параметры
зонда НЕ входят в CE-граф (gce=None), и оба маскирующих маршрута обнуляют его —
align: p.grad = zeros при gce=None; bypass: sign-маска (0·gb>0)=False. При
meta_head_grad=False граф терма не покидает meta_head (тест через autograd.grad:
все градиенты ствола None). scripts/probe_introspection.py: AR-стриминг
(prefill + L=1, state несётся), инъекции токенные и латентные (forward-hook),
локальный z-детектор (rolling median/MAD — глобальная σ ловит дрейф как спайки),
FAR/AUC/задержка, ridge-проба R²(h→сигнал) без обучения ствола + R² обученного
MetaHead. Запускается офлайн на best.pt, GPU прогона не трогает.

**Мелкий режимный фикс по ходу**: телеметрия головы _last_lacuna_rel/_gate/
_lacuna/_ph_sat обновляется теперь в ОБОИХ режимах (раньше eval читал значения,
застывшие с последнего train-шага: stale eval-логи и stale _lac в eval).
M8-изоляция running-статистик (ell_ema/лестницы/банк) не тронута. Eval-_lac
стал живым ⇒ val на следующем резюме может слегка сдвинуться — следить.

**Реестр P3-1**: +3 записи (inner_eye — только при существующем модуле;
chi_temper/chi_sal — только у ВКЛючённых рук: выключенная рука и так identity,
мерить её = шум в DORMANT).

**Ноутбук**: закомментированные A/B-строки всех четырёх флагов + команда
probe_introspection (cell 4).


## P0-2/F3 — критическая находка на живом резюме: tie-градиент за флаг `tie_grad`

Резюм на 162cc20 (7040) дал ожидаемо идентичный CE (4.3594 — forward инвариантен),
но **взрывной транзиент за 55 шагов**: h_norm 160.9 → 2461 (×15), u_max 14.5 → 146,
spk_sat 0.03 → 0.66, g_wo ×18 (4.76e-05 → 8.83e-04), g_l1_proj ×250, branch_r_mirror
×10 (19 → 195), mb overwrites ×90, intent_eff 18 → 1.3. Причина — **F3-mirror**:
градиент реконструкции (измеренный ещё отчётом ‖dL/dW_out‖≈51.5), 11k шагов
уходивший в никуда, теперь течёт в W_proj — ровно та тропа, о которой патч
предупреждал («запускать только с tie_grad=True с начала или с явным A/B»).
Проверено локально по чекпойнту 7040: `_vsa_log_param` = [5.01, 2.88, 2.64, 2.60],
per-layer τ_s = τ_s_base·τ_l/τ_mid; кламп P0-1 связывает на мелких слоях
(τ_s 0.33 → 1.0, decay 0.048 → 0.37) — но это ×1.5 к массе, не ×15 ⇒ доминанта
транзиента — F3, не τ-кламп.

Решение: `cfg.tie_grad: bool = False` (default) — прежнее чтение W_out/буфера
(значения побитово те же: хук/буфер синхронизированы с W_proj^T; градиент
выходного пути снова мёртв, как и 11k шагов). In-graph tie включается флагом
осознанным A/B-плечом (с ноутбука — закомментированная строка). Тесты
`test_f3_ties.py` переписаны на оба режима (+2 → 614). F2-кламп (корректность)
остаётся всегда: его транзиент ограничен и неизбежен.


## Самоорганизация по глубине (analyze 10560 vs 3080)

Модель сама выбрала распределение ролей по слоям — оно видно одновременно в
четырёх независимых каналах и согласовано между ними:

- **L0–L4**: часто-приоткрыты (gate mean 0.10–0.13), низкий пик (0.67–0.69);
  приватная память живёт в L3–L4 (pm_norm 1.38 / 0.88).
- **L5–L9**: двигатель — пик 0.65–0.81 (L7 0.81), максимум обучения весов
  (W_std dev +0.0057…+0.0011), редкие полные вспышки (mean 0.02–0.08).
- **L10–L19**: тихая переходная зона (пик 0.47–0.66, mean 0.04–0.07, pm_norm ≤0.11).
- **L20–L23**: событийный арбитр — редко, но полно (пик 0.66–0.73, mean 0.05–0.11),
  mat gate максимальный (0.62–0.64), pm_norm L23=0.89, веса растут медленно.

Чтение: высокие τ получают медленное/редкое/полное вмешательство, быстрые —
частую мелкую модуляцию; L10–L19 — граница между режимами. Это не назначение
извне (τ-LLRD и гейт-авторитеты задают лишь геометрию), а выученный баланс:
между 3080 и 10560 картина сместилась (wake WATCH→WAKE, gate max 0.730→0.808,
mat gate mean 0.20→0.41) — распределение эволюционирует.

Практический вывод: слой-специфичные вмешательства отложены — «долина» L10–L19
выглядит переходной зоной, а не резервом; глубокий конец уже активен как арбитр.
Лиды плато — головные (log_temp 0.019→0.281, pair-канал rank-16 на резюме).


## analyze 13640 vs 10560 (отчёт HTML: checkponts/best 13640_13640_report.html)

| Метрика | 10560 | 13640 | Тренд |
|---|---|---|---|
| best_val | 7.9828 | **7.9260** | ✓ |
| MLP W_std dev | +0.0011 | +0.0010 | WAKE удержан |
| worst gate max | 0.808 (L7) | **0.865 (L7)** | ✓ |
| mat gate mean | 0.412 | **0.605** (max 0.805) | ✓ рампа |
| **pred (aux)** | 17.0 | **5.87** | ✓✓ зеркало восстановилось |
| max predMSE L7 | 111.3 | **51.8** | ✓✓ аномалия рассосалась |
| max ‖hp‖ | 9.44 (×4.4) | **4.87 (×2.2)** | ✓ нормы успокоились |
| min gate | L3 0.0018 | L19 0.0694 (var 0.06) | ✓ L3 ожил |
| bus cos | 0.493 | **0.286** | ✓ |
| stencil ‖W‖ | 37.12 | **42.68** | ✓ |
| **log_temp** | +0.2807 | **+0.3098** | ⚠ монотонный рост |
| salience H/Hmax | 1.000 | 1.000 | ⚠ плоская |

Выводы: (1) аномалия predMSE/‖hp‖ 10560 была транзиентом — зеркало
восстановилось само; (2) гейты открылись шире (L7 0.865), ранее мёртвый L3
ожил; (3) рампа зрелости идёт (0.41→0.605); (4) единственный монотонный
негатив — log_temp (+0.28→+0.31), плюс плоская салиентность головы.
