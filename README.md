# EVA-CLM

### Единая Вычислительная Архитектура — когнитивная языковая модель без внимания

**VSA-память · кодовый словарь · единое τ-поле · групповые зеркала · обучение как непрерывный процесс**

```
      ┌──────────────────────────────────────────────────────┐
      │                       EVA-CLM                        │
      │     слово → КОД → суперпозиция → τ-иерархия → слово  │
      │                                                      │
      │   24 слоя · D=2560 · 32 эксперта · 211.4M весов      │
      │   словарь 65 536 при нуля параметрах эмбеддинга      │
      │   контекст без KV-кэша · динамика без magic-чисел    │
      └──────────────────────────────────────────────────────┘
```

> Исследовательская архитектура вокруг одного тезиса: **языковое моделирование —
> не умножение матриц внимания, а управляемая временем суперпозиция кодовых
> состояний.** Всё в этом документе — следствие этого тезиса.

---

## Содержание

1. [Три допущения трансформера, которые здесь отрицаются](#1-три-допущения-трансформера-которые-здесь-отрицаются)
2. [Три опоры EVA](#2-три-опоры-eva)
3. [Карта модели](#3-карта-модели)
4. [Слово как код, а не как строка](#4-слово-как-код-а-не-как-строка)
5. [Bind: связывание, сохраняющее норму](#5-bind-связывание-сохраняющее-норму)
6. [VSA-память: четыре темпа понимания](#6-vsa-память-четыре-темпа-понимания)
7. [Cognitive Mirror: слой как совет экспертов](#7-cognitive-mirror-слой-как-совет-экспертов)
8. [Спектр и GroupedMLP](#8-спектр-и-groupedmlp)
9. [SemanticBridge: модель, которая слышит себя](#9-semanticbridge-модель-которая-слышит-себя)
10. [Intent-шина: нить внимания без окна](#10-intent-шина-нить-внимания-без-окна)
11. [Концепты: UCL и банки L1/L2](#11-концепты-ucl-и-банки-l1l2)
12. [Logit Cache: прошлое в кодовом пространстве](#12-logit-cache-прошлое-в-кодовом-пространстве)
13. [SigmoidCodedHead: ответ без softmax](#13-sigmoidcodedhead-ответ-без-softmax)
14. [Рассуждение в скрытом пространстве](#14-рассуждение-в-скрытом-пространстве)
15. [Зрелость: когда слой имеет право голоса](#15-зрелость-когда-слой-имеет-право-голоса)
16. [τ-поле: единица измерения архитектуры](#16-τ-поле-единица-измерения-архитектуры)
17. [Обучение: из чего состоит сигнал](#17-обучение-из-чего-состоит-сигнал)
18. [Балансировка градиентов без магических весов](#18-балансировка-градиентов-без-магических-весов)
19. [Оптимизайзер и планировщик, синхронные модели](#19-оптимизайзер-и-планировщик-синхронные-модели)
20. [Дисциплина длинного прогона](#20-дисциплина-длинного-прогона)
21. [Инференс как часть обучения](#21-инференс-как-часть-обучения)
22. [Цена: против трансформера по всем осям](#22-цена-против-трансформера-по-всем-осям)
23. [Что здесь доказывается](#23-что-здесь-доказывается)
24. [Как посмотреть своими глазами](#24-как-посмотреть-своими-глазами)
25. [Состояние (свежий прогон)](#25-состояние-свежий-прогон)

---

## 1. Три допущения трансформера, которые здесь отрицаются

**Контекст — таблица пар (K, V).** Каждый шаг дописывает столбцы матрицы:
память линейна длине, стоимость внимания квадратична. Десятилетия индустрия
оптимизировала *следствие* (flash-attention, скользящие окна, KV-квантование),
не переспрашивая причину.

**Словарь — строки большой матрицы.** Эмбеддинги и голова стоят |V|·D: при
vocab 65 536 и D 2560 это ~335M параметров *только на перевод id ↔ вектор*.
Редкие токены недообучены, частые перекормлены, а градиент протекает через
softmax — функцию, ревнивую по определению: рост вероятности правильного
слова насильно принижает все остальные.

**Время — набор подобранных чисел.** Скорости EMA, температуры гейтов,
задержки разморозки, LR-decay по глубине — десятки констант без общей природы,
которые рассогласуются при любом росте модели.

EVA отрицает все три: **контекст — вектор суперпозиции, словарь — код, вся
динамика — функция одной величины τ.**

---

## 2. Три опоры EVA

**Память как суперпозиция (VSA).** Гиперполии (Plate, 1995): множество
объектов хранится одним вектором — суммой случайно повёрнутых элементов;
развязка корреляцией с элементом. O(1) на запись, ёмкость — порядок
размерности, деградация — плавная и «размытая», как у человека. В EVA этот
принцип несёт всю контекстную ветку: ни одной K×V таблицы в модели нет.

**Время как иерархия (τ-поле).** Кора мозга устроена так, что верхние зоны
меняются быстро, нижние — медленно. По глубине EVA идёт лестница постоянных
времени τ₁…τ₂₄, и из неё как из *единственной физической размерности*
выводятся скорости всех EMA, температуры гейтов, задержки созревания, профили
градиентного клипа и LR. Искривлять геометрию может один обучаемый параметр
на слой — всё остальное функции.

**Знание как код (sparse block codes).** Токен — не строка таблицы, а булев
код веса 6 из 32 бит; 65 536 слов живут в C(32,6) = 906 192 конфигурациях.
Вывод — не argmax по softmax, а 32 независимых вопроса «бит активен?».
Градиент на ошибку собирается побитно: модель учится *сборке ответа*, а не
конкуренции вероятностей. Цена словаря по параметрам — ноль.

Опоры перемножаются, а не сосуществуют: кодовое пространство — общий язык
для связывания, кэшей и головы; τ решает, где и как быстро эти коды имеют
право перезаписываться.

---

## 3. Карта модели

```
 токен id
    ▼
 Эмбеддинг: код → плотный микс → базис ⊕ RoPE          0 параметров от vocab
    ▼
 UCL (концепты-прототипы) + Банки L1/L2 (фраза/период)  контекст до стека
    ▼
 ×24  EVABlock:  conv → bind → VSA-память(4 темпа)
                 → mirror(32 эксперта) → спектр → GroupedMLP
      поперёк: SemanticBridge (глубина) · intent-шина (время)
      вглубь:  лестница τₗ · зрелость Mₗ · переменная точность
    ▼
 Logit Cache: горизонт 512 токенов, отбор по новизне, кодовая проекция
    ▼
 SigmoidCodedHead: 32 бита + factorized branch
    ▼
 CE по каждой позиции + aux-симфония → спектрально-выровненный градиент
```

Поток не «слева вверх»: мост и intent носят сигналы и вниз (top-down),
память и кэш — поперёк времени, зрелость — поперёк глубины.

---

## 4. Слово как код, а не как строка

Каждому id сопоставлен детерминированный блок-код (инкрементальный отбор по
растущему хешу — дискретизация без обучения):

```
c ∈ {0,1}³²,  ‖c‖₀ = 6
z = σ(2 · M c)          M — фиксированная ортогональная матрица 32×32
e = z ⊗ B               B — базис сегментов; сегменты 1:1 с экспертами зеркал
e ← RoPE(e)             θ = 1e6, вращение по позиции
```

Плотный микс `z` превращает разреженный код в «запах» слова: похожие коды
автоматически дают близкие векторы, а пересечения бит между любыми парами слов
имеют *заданное* статистическое распределение — ровно то, что делает
гиперполию хорошо обусловленной. Отсюда: стоимость словаря — ноль, обобщение —
через пересечения кодов (два слова с двумя общими битами уже делят геометрию;
редкие слова не «недообученные строки», а комбинации выученных бит), и одна
решётка кодов служит всем интерфейсам модели сразу — привязке, кэшу, голове.

---

## 5. Bind: связывание через фазовую спираль

Два базовых оператора VSA в модели — не групповое умножение на кругу, а
вложение в комплексную спираль с управляемой круткой:

```
u, v = сплит(h)                                    пары координат D → K
θₛ  = ωₛ · freq_scale · RMSNorm(Wp·h) + βₛ         обучаемые частоты и фазы
u'  = u·cos θₛ − v·sin θₛ ,   v' = u·sin θₛ + v·cos θₛ
bind = Σₛ Wₒ·(u'ₛ ⊙ v'ₛ)                           суперпозиция масштабов спиралей
```

Поворот — изометрия: ‖(u′,v′)‖ = ‖(u,v)‖. **Градиент не домножается на
случайную матрицу 24 раза подряд** — он проходит через вращение, сохраняя
норму: глубокий стек жив без танцевальных костылей вокруг нормализаций.

Изометрична именно поворотная подоперация. Полный bind — поэлементное
произведение двух повёрнутых копий и обучаемое Wₒ — билинейная форма:
усиление зависит от входа и весов, и разумный разброс усиления удерживают
регуляризаторы branch (равенство log-дисперсий
ветвей), а не конструкция. Это осознанная цена ёмкости. (M64.6: регуляризатор
nuc удалён как инертный — см. тумбстоун в losses.py.)

Второй принцип — **когерентность фаз**: величина |Σₛ e^{iθₛ}|/(S·d) заметна
только там, где спирали складываются в фазе. Состояние с подходящей
«частотной подписью» усиливает само себя в потоке; чужие фазы взаимно
глушатся. Селективность вырастает из физики, а не из жёсткого top-k.

---

## 6. VSA-память: четыре темпа понимания

Каждый слой держит четыре рекуррентных регистра с постоянными, привязанными
к τ слоя:

```
τ_s(l) = base_s · (τ_l / τ_mid),        base = 1, 4, 16, 64

d_mod   = min( σ(hₜ·w_d + b_d) / σ(b_d), 1 )      # покой = 1: контент лишь укорачивает память
penₜ    = фактор отклонения ошибки предсказания от её EMA-базы
decayₜ  = max( e^{−1/τ_s} · d_mod · penₜ,  e^{−k/τ_s} )   # лестничный floor τ_s/k, k=2
gateₜ   = softplus( hₜ·w_i + b_i + γ·‖hₜ − p̂ₜ‖ )
memₜ    = decayₜ ⊙ memₜ₋₁ + gateₜ · hₜ
```

* Быстрый регистр держит синтаксис фразы, медленный — тему документа.
* Множитель `σ(h·w_d)` делает забывание **контекстным**: на границе мысли
  регистр открывается шире.
* Член с ошибкой предсказания — примитив удивления: неожиданное записывается
  сильнее. Память здесь — функция несоответствия ожиданию (предиктивная
  обработка в чистом виде), а не буфер «последнего».
* Чтение — гибрид адресности и мягкости: `σ(scores)·(1 + softmax(scores/τ))`.

Рекурсия считается chunked prefix-scan'ом с привязкой к хвосту чанка:
интра-чанк суммы пишутся как exp(L−A)·cumsum(b·exp(A−L)) — все операнды
ограничены |b|, fp32-устойчивы и в forward, и в backward. Скан векторизован
одним вызовом по контурному layout (B·n_c, 32, S, D) — бит-в-бит эквивалентен
почанковому циклу. Параллельно на обучении, O(1) памяти на инференсе.
Никаких K×V — опора 1.

---

## 7. Cognitive Mirror: слой как совет экспертов

D=2560 разрезан на G=32 сегмента-эксперта. Каждый ведёт буфер из k эталонов
(staircase: 8 у поверхностных слоёв до 32 у глубоких) и судит о своём праве
говорить по пяти сигналам:

```
t — темп-согласие с регистром      p — точность предсказания следующего шага
s — гладкость (EMA с собой-вчера)  y — симметрия связки с парным сегментом
h — helpfulness: вклад в движение h против общего
```

```
gₑ  = σ( w·|Δₑ| + b + grad_mod + contradictions + meta_trust )
выход слоя по зеркалу = Σₑ gₑ · readₑ(bufferₑ)
```

Это MoE с **самооценкой по факту, а не по роутеру**: эксперта повышает его
предсказательная история, понижает — противоречие стволу и стагнация;
`meta_trust` даёт уверенно-непохожему эксперту право голоса выше своего
среднего (антихрупкость совета).

У каждого эксперта — **приватная память**, записываемая только из зрелого состояния
(`conf = σ(−‖Δ‖)`): опыт консолидируется посегментно, как миелинизация.
Чтение точного буфера включает переменная точность: STE-гейт
`precision = σ(gate(h))`, обучаемый сквозь CE — слой сам выбирает, где
достаточно грубого регистра, а где нужен точный след.

---

## 8. Спектр и GroupedMLP

**Спектральная ветвь**: сегменты h через фиксированный DCT-базис с
по-частотным множителем `λₖ` (обучаемый, модулируется τ-лестницей) — слой
решает, сколько высокочастотной (локальной) и низкочастотной (тематической)
структуры пропустить дальше. Осцилляторная настройка без осцилляторов.

**GroupedMLP** — SwiGLU ×32 группы ×4 расширение, но не слепой FFN: его
ворота спрашивают совет у зеркала и моста:

```
mod = σ(a + b·mirror) · usefulness
glu = σ(gain)·σ(Wg·f)·σ(Wv·f)                 живой смысловой гейт
out = Σ_f SwiGLU_f(x)·(1 + β·(2·glu−1))       при τ-якоре 1.5·σ на init
```

В EVA MLP — исполнитель, согласованный с памятью, а не «ещё один перцептрон».

---

## 9. SemanticBridge: модель, которая слышит себя

Стек блоков обычно передаёт только *результат* слоя, теряя его *намерение*.
Bridge вводит сквозной канал глубины:

```
sₗ = probeₗ(hₗ)                            семантический «выдох» слоя (256-мерный)
инъекция: hₗ₊₁ += tanh(c)·Proj(streamₗ)    stream — смесь соседей по глубине,
                                            горизонт смешения = τ_norm(слой)
время:    bridge_stream ← EMA(sₗ)          персистентная нить между шагами
самообучение: InfoNCE — sₗ[t] отличает Emb(tok[t+1]) от пула окна;
  цель центрирована и откреплена (устойчивый предиктивный сигнал
  без вырождения в константу)
```

Мост **обучается предсказывать эмбеддинг следующего слова из смыслового
выдоха текущей глубины**: если выдох действительно несёт смысл — он обязан
быть предиктивным. Это dense-супервизор на каждую слой (~2M параметров на
связь целиком) и канал, которым смысл перетекает *мимо* остаточного стека:
резонанс глубины, не доступный градиентному конвейеру.

**Свежий прогон (10450):** параметры моста обучаются (bus_head_proj 0→34,
intent_probe W=169, w_intent L12=2.53), но контрастив-цель **не учится** —
`bridge_conn` весь прогон ≈ ln(383)=5.95 (шанс). К 10450 salience-сигнал
равномерен (H/Hmax=1.0), intent-поток раздут (mean 5.6e4, max 1.4e5).
Читает — да; фокусируется — нет. Канал «построен, но не подключён»;
управление — M65 (валюта/UCB), не полы.

---

## 10. Intent-шина: нить внимания без окна

Модели нужно не только «что было», но и «о чём я сейчас». Intent-шина —
восходяще-нисходящий поток намерений, живущий в темпе своего слоя:

```
freshₗ    = σ(probe(hₗ))                             локальное намерение
carriedₗ  = aₗ·carriedₗ(t−1) + (1−aₗ)·freshₗ(t)      aₗ = 1 − 1/τₗ
bus       = fresh + carried        top-down:  gate_logits += salience·w_sal
```

Быстрые слои обновляют намерение почти на каждом шаге; медленные — «едят
слона», удерживая сквозную цель через сотни шагов. Это второй неоконченный
канал модели (первый — VSA-память): **размытое понимание + направленная
нить** — те две вещи, которые attention заменяет квадратичной стоимостью.

---

## 11. Концепты: UCL и банки L1/L2

**Unified Concept Layer** — глобальный слой из S=8 прототипов-концептов,
живущий сразу за эмбеддингом:

```
d = min_c ‖e − c‖ · новизна-порог (обучаемый разрыв)
рождение: уверенный + зрелый + новый паттерн становится прототипом
запись:   через index_copy — дифференцируемо, концепты обучаются CE-путём
чтение:   soft-attention по прототипам → модуляция всего стека ниже
```

Концепт умирает, если не востребован дольше своего τ-горизонта. UCL пишется и
на инференсе (§21) — «субъект, который узнаёт новое, не переставая говорить».

**Свежий прогон (10450):** запись жива — births=3, max_conf=0.60, слоты 7/8,
41 537 записей; но **чтение модель закрыла**: read_scale σ(w)≈0.02. Пол
M64.1 (репараметризация `fl + (1−fl)·σ(w)`) держал канал ≥0.1 до step 5000,
после релиза модель закрыла его заново (как и предсказано M59). Гигиена
M65 — валюта/UCB (стимул держать канал), а не ещё один пол.

Под ним два буфера плавного распада:

| буфер | горизонт | запись |
|---|---|---|
| L1 | последние 3 предложения | на границе фразы |
| L2 | 32 обучаемых слота «периода» | по зрелости слоя ≥ θ |

Чтение — до стека: каждый слой начинает не с нуля, а с ощущения контекста.

---

## 12. Logit Cache: прошлое в кодовом пространстве

Стек живёт окнами по 256, понимание требует прошлого. Кэш держит **горизонт
в 512 токенов — верхнюю шкалу VSA-лестницы** (точное окно и размытая
лестница покрывают один динамический диапазон) и даёт стеку спросить у них:

```
запись:  h детачится; его k,v кодируются ОДНАЖДЫ при записи
         (k = k_norm(k_proj(h))) — память как записанный код, а не пересчёт

спрос:   q = Q(нынешнее окно) по записанным (k,v) + живой хвост
вплёт:   h' = h + σ(gate(h)) · attn_out
```

* **Write-time encoding** — стоимость шага O(seq): прошлые окна закодированы
  весами того момента, когда были записаны, — как и положено памяти.
* **Отбор по новизне, а не по возрасту:** заполнение оценивается surprisal'ом
  зеркал (то, что лестница не может сжать), вытеснение — минимальным
  retention = новизна·exp(−возраст/τ); новейшая запись не вытесняется никогда.
* **Кодовое пространство вместо словарного** (инференс): логиты 65 536-мерного
  поля сжимаются в *родной язык модели* — per-bit evidence кодовой головы:

```
profile = tanh(logits/10) · codesᵀ / S        V→32, 0 параметров
все карты — 32×D вместо 3×V×D:  −500M «роста»
```

* **Инициализация = тождество:** гейт стартует с σ(−10) ≈ 0 — модель начинает
  с «прошлое не нужно» и **покупает память только если CE докажет её
  полезность**. 43× экономия против KV-кэша на 1M токенов (≈5.5GB против 240GB).
* **Scheduled sampling (5%) — живой** (M56): `observe_output` кладёт detached-логиты шага
  в одношаговый стэш (та же конвенция, что у salience), и следующий форвард отдаёт их кэшу —
  с вероятностью 5% запись/чтение идёт **сжатыми логитами** (инференс-представление), и
  прогоняется путь `bit_profile → logit_to_hidden`. Модель тренируется против того
  представления, с которым встретится на деплое; счётчик `_r1_steps` едет в чекпойнт.
  Очистка на границах документов и eval; eval остаётся в h-режиме (изоляция).

**Свежий прогон (10450):** кэш по-прежнему identity — σ(−10), **h-entries=0**
(при 526 R1-срабатываниях — ни одной записи в шортлист не легло). «CE не
покупает» — как задумано; эксперимент: поднять sampling до 0.15–0.20 на
первых 20k (и отдельно разобрать, почему записи не рождаются).

---

## 13. SigmoidCodedHead: ответ без softmax

Голова задаёт 32 независимых вопроса «активен ли бит?» и собирает слово:

```
zₖ = ⟨hₖ, readoutₖ⟩/T + b_bit + b_tok       двойной bias: бит и токен — разные смещения
logit(w) = Σₖ codes[w]ₖ·zₖ − base          свёрточный скор по пересечению кода с битами
NLL(w*)  = Σ_{k∈w*} −log σ(zₖ) + Σ_{k∉w*} −log(1−σ(zₖ))     Бернулли по каждому биту
```

Почему это другая физика, а не «softmax на минималках»:

* **Градиент не ревнив.** Ошибка в трёх битах — штраф по трём битам,
  остальные 29 не трогаются. Softmax неверным ответом *обязательно* ломает
  вероятности всех правильных; здесь ошибка частична — частичен и ремонт.
  Это прямой ответ на хвост редких слов словаря.
* **Неопределённость lossless**: σₖ = 0.5 по всем битам — не «равномерно по
  65 536», а честное «биты не решены»; из неё читаются целые множества
  слов-кандидатов по пересечению кодов.
* **Factorized branch** — вторая декомпозиция той же цели: предсказание битов
  по подпространству сегментов; `CE + branch` обучают код надёжнее одного CE.
* NLL снимается по **каждой** позиции окна — плотность сигнала максимальна.

### 13.1 Гибрид σ×softmax: аудит и финальная форма

Одна формула на все гейты архитектуры:

```
gate = σ(z) · (1 + softmax(z/τ))      сигмоид решает «участвует ли», софтмакс — «насколько охотно»
u    = z + λ·(log1p(r) − log1p(1/K))  лог-режим головы: акцент входит как log-odds, не как множитель
```

Аудит (M52a) вскрыл тихие дефекты и закрыл их:

* **Мёртвый `base`.** В нормированном пути (logsumexp по словарю) константа по словарю
  сокращается *точно* — градиент ровно ноль. Убран: «неизвестное» живёт в лакуне (13.2).
* **Акцент был заражён прайором.** Софтмакс читался с `z + bit_bias` — `corr(bit_bias, бонус)=+0.90`,
  прайор сам себя усиливал. Теперь акцент читает только данные `z/T`.
* **λ — обучаемый** (init 1): модель сама решает, сколько конкуренции ей нужно.
* **Насыщение σ.** При |u|>17 σ′(u) в fp32 = 0: CE-градиент к голове умирает (~9e-14) — а с ним
  и ствол (голова — единственный путь CE к транку). ST-клампы спасают экспоненциально медленно;
  работает только **линейный барьер**: `L_wall = w·relu(|u|−6)²` — градиент `2w(|u|−6)` жив при
  любом u (1.9e-4 при u=30).
* **τ-клампы** (`exp(log_temp).clamp`) замораживали температуру — заменены straight-through:
  форвард тот же, обратный проход — тождество.

### 13.2 Лакуна: неизвестное как свойство, а не эвристика

Readout покрывает K=64 направления из D=2560 — значит, у любого состояния есть ортогональный
остаток, которого биты **не видят по построению**:

```
e_l = h − Σₖ zₖ·Rₖ           ⟨e_l, Rₖ⟩ = 0 для всех k (проверено 7e-7)
ℓ   = ‖e_l‖ / ‖h‖             ~0.97 для ЛЮБОГО реального состояния (пол ≈ √(1−K/D))
novelty = ℓ / EMA(ℓ)          само-калибровка: ~1 на текущем уровне, >1 на всплеске
g_l = σ(30·(novelty−1) − 3)   гейт фантомов: 0.05 закрыт в норме, >0.9 на всплеске
```

Лакуна не детектируется — она *существует*: известные биты математически слепы к остатку.
Фантомный базис (K_p=32, ортонормированный) читает остаток; обучаемый шум η (init 0.05,
отдельный генератор — глобальный RNG не сдвигается) учит канал извлекать структуру из шума.
Zero-init mix ⇒ форвард стартует бит-в-бит как «только известные биты».

### 13.3 SRL: разрешение состояния

«Прогнать состояние до ясности» формализовано как аннилинг-EM по словарю кодов:

```
E-шаг:     p(v|u) ∝ exp( u·(2C_v−1) / τ_t )   софтмакс тянет ближайшее известное
M-шаг:     ĉ = Σ p·C_v                        ожидаемый код
уточнение: u ← u + α·(logit ĉ − u)            «известное + то, с чего начал сигмоид»
τ_t = τ0·γ^t                                  разрешение растёт
```

Исход классифицируется (проверено: чистый код → conf 1.0 / expl 0.03 нат-на-бит; смесь двух
кодов → conf 0.50; шум → expl 1.08):

* **концепт** — max_p>0.9 и expl<thr;
* **противоречие** — расщеплённый постериор (max_p≤0.9);
* **лакуна** — цена объяснения expl ≥ thr (биты пришлось бы переписать).

Шортлист 64 кандидатов из первого прохода; прогрев до 1045 (на init петля коммитится
к случайным кодам и удваивает CE — измерено: 11.47 → 22.39).

### 13.4 Фантом-банк и лайфцикл

Повторяющиеся направления лакуны — кандидаты в новые концепты. Банк (M54) держит до 16
направлений с cosine-слиянием (0.7), накапливает confidence по повторяемости и применяет
лайфцикл EVA-Ai: **confirm 0.75 / archive 0.25**. Подтверждённые направления — кандидаты
в консолидацию (рост бит, M55). Буферы едут в чекпойнт.

**Свежий прогон — цикл отработан впервые:** рождение на 7590 (ph_obs=1, conf 0.49),
взрыв 8030–8140 (16 фантомов, 17 births, 4 confirmed, merged 87–101, dropped 35–119),
консолидация 9515 (1 фантом, **17 в архиве**). Итог: 18 births / 23 obs / 102 merged /
120 dropped / 17 archived. Важно: рождения шли при `lacuna_rel` 0.67–0.75 (**<1**) —
порог 1.1 не подтвердился; реальный триггер — открытие глубины 16 + рост `conflict`
(~0.3). Калибровка порога — M65.

### 13.5 Контур памяти

* **Противоречие голова↔память**: направление чтения банка памяти сравнивается с направлением,
  которое подразумевают биты (`Σ σ(uₖ)·Rₖ`); конфликт мягчит логиты `logits/(1+k·χ)` — модель
  меньше уверена там, где предсказание расходится со свидетельством памяти.
* **Расширение поиска по лакуне**: относительный всплеск novelty масштабирует температуру
  внимания банка (`temp·(1+k·rel_excess)`) — большой разрыв расширяет поиск.

### 13.6 Позиционирование (prior art, 2026)

Компоненты стоят на известной земле: сигмоид-внимание с ограниченными производными (Apple, 2024;
ICLR'25 про attention sink), композиционные/хэш-головы, бьющие softmax на 100M–3B
(MultiHashFormer, EMNLP'26), null-space OOD-проекции, COBWEB-формирование понятий (CobwebTM, 2026).
Связка **структурная лакуна + SRL с классификацией исходов + лайфцикл и рост** прямых аналогов
в литературе не имеет.

---

## 14. Рассуждение в скрытом пространстве

До головы — петля, где модель думает в том же D=2560, не тратя токены:

```
для r = 1…R(t):                               R адаптивно: средний гейт < 0.5 → выход
    a = sigmoid-attention(q(h), буфер витков)  множество тем, не одна
    h ← h + tanh(g_r)·read(a)                 g_r ∈ [−1,1]: отрицательный = антизнание
                                               («эта ветка прошлого — ложь, вычти»)
ramp: сила петли → 1 − exp(−t/1000)           рассуждение входит в силу постепенно
```

Буфер витков — сам VSA-регистр (O(1)), а **Triad** на инференсе даёт рассудку
право переспросить ствол целиком при низкой уверенности (`conf < θ` → второй
проход, консервативный бленд 0.5/0.5) — без единого нового параметра.
Рассуждение цепочками токенов — это внешняя костыльная версия того же; здесь
оно внутреннее и в своей валюте.

**M53d — применение отключено by default:** `head_srl_apply=False` (config).
На 1045-постмортеме жёсткий refine убил прогон (ce 11→31→78→91, sat 1.0):
`u ← u + 0.7·(logit c − u)` на каждом форварде стягивает биты к постериору
кода намертво, CE-градиент не может бороться. По умолчанию SRL считает
телеметрию (концепт/противоречие/лакуна) без изменения форварда;
`apply=True` — только эксперименты. Свежий прогон: гейты петли 0 при
apply=False, A/B `natural > FULL` — подтверждение вреда refine.

---

## 15. Зрелость: когда слой имеет право голоса

Слои включаются не расписанием, а созреванием:

```
Mₗ(t) = max( σ((ln t − ln T_eff,ₗ) · T_eff,ₗ/Δt) ,  готовностьₗ )
T_eff,ₗ = T₀ + α·(1 − τ_normₗ) · T_задержки
```

Готовностьₗ считается из насыщения спада pred-ошибки зеркала
(sat = 1 − EMA[pen]/pen_init, сигмоида); мост измеряется отдельно и служит
доверием (trust) в оптимизаторе для группы bridge_glu. Рампа — log-time:
локальный наклон сохраняется при t ≈ T_eff, и градиент по времени не умирает
на больших t. Право писать память, модулировать MLP и впрыскивать мост
масштабируется Mₗ; замороженный слой не молчит — его базовый гейт ≈ 0.667
оставляет щель для обучающего сигнала.

**Измеренно (свежий прогон, 10450):** волна пробуждения — **middle-first**, не
deep-first и не bottom-up: maturation-гейт **L4–L8 = 0.92** (пик), **L0–L3 =
0.18–0.22** (дно — стали проекционными, `mod_mlp` 0.52→0.07), L9–L23 растут
0.44→0.63 (рампа T0=8000/delta=4000 идёт до ~12000). Вглубь — depth-контроллер
открывает ступени 8→12→16→20→24 по плато-детектору; каждое открытие совпало
с улучшением val. Gradient-профиль при этом **deep-heavy**: depthgrad
L0/Llast=0.08, L23=702 (ядро CE-градиента у последнего слоя). Возраст — время;
зрелость — право; τ — связь между ними.

---

## 16. τ-поле: единица измерения архитектуры

```
Δ₀ = ln(τ_max/τ_min)/(L−1),    dev_eff = dev_max·tanh(dev/dev_max)
τₗ = exp( ln τ_min + ln(τ_max/τ_min) · cumsum(Δ₀·softplus(dev_eff))ₗ / last )
```

Нормализация на кумулянте жёстко держит правый конец: τ_{L−1} = τ_max = 512.
Левый при нулевых отклонениях подъедается дискретизацией: τ₀ ≈ 9.5,
фактический размах при инициализации ~54×. Быстрые слои — поверхность
и синтаксис, медленные — тема; и **все** темпоральные константы модели
выводятся из лестницы:

| Что | Формула |
|---|---|
| EMA-горизонты (VSA, intent, bridge) | `α = 1 − 1/τₗ` |
| задержка созревания слоя | `T₀ + (1−τ_norm)·T_delay` |
| температуры гейтов | геометрическая интерполяция τ_min^g…τ_max^g |
| целевая скорость записи памяти | `i_target = min(1, c/τ)` |
| LR по глубине | `(τₗ/τ_ref)^(−γ)` |
| сила клипа AGC | `c·(τ_ref/τₗ)^γ` |
| частоты спирали bind | `(τ_min/τ_max)^(τ_norm·η)` |
| спектральное демпфирование | `cos(π·τ_norm/2)` |

Ни одной независимой константы. Хочешь замедлить модель — подними лестницу,
и четырнадцать механизмов сдвинутся **вместе**, как стрелки связанных часов.
Искажение геометрии — один обучаемый вектор `devₗ`. Это и есть «единая
вычислительная архитектура»: одна размерность времени вместо набора чисел —
как метр в СИ.

---

## 17. Обучение: из чего состоит сигнал

Главная потеря — побитовый Бернулли-NLL кода правильного слова (§13) по каждой
позиции. Вокруг — aux-симфония, и у каждого голоса τ-обоснование:

| сигнал | что требует |
|---|---|
| `pred` | регистр должен предсказывать следующий шаг |
| `bridge_conn` | смысл слоя должен предсказывать следующее слово |
| `gate_l1` | гейты зеркал — разреженность |
| `diversity / div / decorr` | сегменты не должны схлопываться в близнецов |
| `reinforce` | эксперта нельзя повышать без предсказательной пользы |
| `balance` | ни один сегмент не узурпирует поток |
| `gradalign` | aux-градиент должен учить гейты, а не мешать CE |
| `novelty / signal_ent / repulse / …` | гигиена суперпозиции |

Aux-сигналы возвращаются из модели **сырыми** — ни одного ручного веса λᵢ.
Всё, что влияет, видно в геометрии градиента; всё, что не влияет, — телеметрия
стража, и она так и помечена.

---

## 18. Балансировка градиентов без магических весов

Aux-сигналы масштабируются не лямбдами из тетрадки, а измеренным
собственным масштабом (M64.4):

```
loss = ce + s · Σaux        s — измеренный масштаб aux-градиента vs CE
s вычисляется раз в 8 шагов (balancer_align_every=8), guard nb ≥ 0.05·na,
кап 10 (bal_sc). Косинус g_CE/g_aux печатается (bal_cos): согласованный aux
проходит, ортогональный молчит, противный — видно в логе.
```

Исторический вариант — проекция `g_aux ← g_aux · max(cos(g_CE, g_aux), 0) ·
‖g_CE‖/‖g_aux‖` — gradalign-хук: **заморожен** (полный backward на каждом шаге
давал 2.5–3× накладных расходов; каденс-путь дешевле при той же честности).
Свежий прогон: bal_s вырос 0.0002→1.17 — к поздней фазе aux-вклад сравнялся
с CE; `bal_sc` упирался в кап 10 (M65: кап 3–4).

---

## 19. Оптимизайзер и планировщик, синхронные модели

**EVAAdamW** — Adam с добавками и *проверенным эталоном* (в чистом режиме
bit-identical torch-AdamW — чтобы сравнивать нечестное с нечестным нельзя):

* **cautious mask**: шаг применяется только когда знак совпал с текущим
  градиентом — экспоненциальная память не тащит назад;
* **radial-подавление (AdamP-null-band δ=2/√D)**: из шага снимается компонента,
  ведущая к вырождению сегментных геометрий;
* **медленные EMA движения весов** — опциональная «подкорка», накапливающая
  траекторию в выпуклом u-пространстве.

**MirrorLR** читает статистику самой модели вместо календаря:

```
mult = ³√(var_ratio · α_ratio · gate_ratio) · mag · loss_factor
```

Движение зеркальных компонент вверх → LR вниз (модель бежит, не подталкивай);
затишье → вверх (можно смелее); улучшение val ×1.05, регресс ×0.5; после
дивергенции — **re-warmup** (прогреть заново), а не «уменьшить вдвое».
Группы LR собраны той же τ-лестницей: глубина × роль × WD — единственные оси.

---

## 20. Дисциплина длинного прогона

Многодневное обучение на арендованном GPU имеет смысл, только если цифрам
можно верить. Что для этого сделано:

**Каждый чекпоинт — всё состояние.** Конверт несёт весь прогон: веса,
оптимизайзер (восстановление по именам переживает смену архитектуры),
планировщик, курсор данных, RNG-потоки, streaming-состояние документа и
цепочки. Два артефакта: `best.pt` — при улучшении val (последний
val-чистый) и `latest.pt` — каждые 495 шагов и на каждой eval. Рядом
дописывается `val_history.jsonl`. Перезапуск продолжает марафон: resume
предпочитает `latest.pt` и стоит не больше 495 шагов. Идентичность
кодовой книги и конфигурации проверяется при загрузке — молчаливый
half-resume невозможен.

**Flush без потери непрерывности.** На каждом роллинг-сейве сбрасывается
то, что не нужно для продолжения обучения: содержимое logit-кэша,
stream-кэши зеркал, диагностические буферы, кэш аллокатора. Веса,
оптимизайзер, курсор и состояние документа не затрагиваются.

**Страж-инструментация без самоуправства.** FailureDetector удалён (M58a):
он был полностью инертен (не подключён ни в train.py, ни в ноутбук —
NaN-потеря делала прогон тихим зомби до следующего eval). Текущая обвязка:

* **AuxKillSwitch** (M64.12) — measure-only по умолчанию: считает и печатает
  `ks_off/ks_low/ks_err` (сколько раз каждый aux был бы обрезан/выключен),
  реальный обрез включается `aux_kill_switch=True` (порог-Шмитт, бюджет
  max_off, тест T12);
* **grad census** — 17 каналов на шаг (`g_wk/g_wv/g_fusion2/head_wall/
  g_readout/g_log_temp/g_ucl_scale/...`) — взрыв банка видно в момент;
* **спайк-снапшот** (`spk_n/spk_u_max/spk_h_norm/spk_sat`) — состояние головы
  на пике;
* **anomaly_track** — пер-слой max ‖hp‖/predMSE/min-gate с дельтами к
  прошлому чекпоинту (считается analyze-ем);
* **Adam-ценз** (`--moves`) — кто реально учится, какие группы застряли
  (свежий прогон: 0 застрявших из 1860).

**Валидация, которую нельзя испортить.** Holdout — 3 файла корпуса на уровне
файлов, навсегда исключённые из обучения; вокруг eval изолируются рабочие
буферы, концепты и кэш — валидационные документы не оставляют следа в памяти
модели. Окна ротируются по границам документов с полным сбросом потоков.

**Ни одного мёртвого механизма.** Автотест ищет параметры с нулевым
градиентом вне явного белого списка с обоснованиями. В системе из ~15
подмеханизмов «молча не работает» — главный способ самообмана; против него
стоит страж.

---

## 21. Инференс как часть обучения

EVA не разделяет «фазу жизни» и «фазу службы»:

* концепты UCL **рождаются из инференса** — уверенный, зрелый и новый паттерн
  записывается без всякого backward;
* регистры и кэш стриминговые по построению: генерация любой длины стоит
  O(1) на шаг;
* logit-кэш живёт обеими половинами dual-mode (h на трейне, кодовый профиль
  на инференсе), scheduled sampling сшивает представления;
* сгенерированные токены возвращаются в модель как настоящие — «речь»
  консолидируется в опыт.

---

## 22. Цена: против трансформера по всем осям

Сравнение при равной ёмкости (D=2560, 24 слоя, |V|=65 536):

| Ось | Трансформер | EVA-CLM |
|---|---|---|
| Память контекста на шаг | KV-кэш O(L·D) | регистры + окна O(D) |
| FLOPs контекста | O(L²·D); обход — окнами | линейный скан + оконный кэш |
| Параметры словаря | ≈ 335M | **0** (буфер кодов) |
| Вывод | softmax, ревнивая конкуренция | 32 независимых бита |
| Константы динамики | десятки ручных | 0: функции τ (1 сдвиг/слой) |
| Рассуждение | цепочки токенов снаружи | петля в скрытом пространстве внутри |
| Обучение на инференсе | нет | концепты, консолидация |
| Живой прогон | — | fp32; A100 40GB: B=1×384 (свежий прогон: 25–35GB, 72–86 tok/s, глубина 8→24); L4/T4: B=1 |

Экономия — не «оптимизация трансформера», а следствие опор: матриц внимания
нет не потому, что их удешевили, а потому что контекст — суперпозиция.

---

## 23. Что здесь доказывается

Ставка формулируется как эксперимент, а не как манифест:

1. **Инструментальный уровень** — механизмы живы и честны: градиенты доходят
   (детектор мёртвых параметров), численные схемы точны (эталонные сверки
   сканов), оптимизайзер верифицирован до побитового совпадения с эталоном,
   длинный прогон resume-безопасен и наблюдаем. *Проверено.*
2. **Идейный уровень** — **быстрее ли EVA учится на токен**, чем
   трансформер равных параметров и вычислений? Отвечает цикл 150k шагов
   (38–77M токенов в зависимости от батча) против matched-vanilla при том же
   бюджете. *Идёт.* Свежий прогон на 10450 шагов: **val 8.2126** против
   последнего старого 8.49@7315; до униграммы корпуса (7.48 нат) — 0.73 ната.
3. **Механизменный уровень** — kill-tests: обнуление каждого U-механизма с
   измерением потери; красота формулы ещё не аргумент нужности. *В плане.*
4. **Уровень существования** — то, что недоступно baseline на этом бюджете:
   retrieval за пределы окна через банки и кэш. *В плане.*

Нулевой разрыв в пункте 2 — результат, а не поражение: система спроектирована
говорить правду.

---

## 24. Как посмотреть своими глазами

```
core/        модули модели (single-source правды: τ-поле, VSA, mirror…)
notebooks/   eva_colab.ipynb — канон прогона: клетки 1→10
scripts/     train.py (CLI-зеркало), analyze.py, generate.py
tests/       461 автотест: численные эталоны, регрессионные локи, детектор
wb/          BPE-токенизатор и утилиты потоков данных
docs/        WHITEBOARD.md (протокол ревью, реестр M64), ARCHITECTURE_JOURNAL.md,
             analysis/M63_SYNTHESIS.md (аудит-цепочка)
```

Старт: Colab, uint16-потоки `token_stream_*_eos.bin` на Drive, клетки 1→10
по порядку. Профиль A100 40GB (свежий прогон): B=1, seq 384; ранее B=2×256.
Рестарт с любой точки — `FORCE_FRESH=False`: resume подхватит `latest.pt`;
полный сброс — `FORCE_FRESH=True`. Конфиг: D=2560, 24 слоя, 32 эксперта,
код 64 бита (twin_free), vocab 65 536, fp32; корпус 1.93B токенов, три
последних файла — вечный holdout. Энтропии корпуса (нат): униграмма 7.48,
биграмма 4.21, uniform 11.09.

**analyze.py** (один прогон — полный разбор + HTML):

```
python scripts/analyze.py checkponts/best.pt --moves --carry --depthgrad \
    --no-gradinfo --seq 128            # по умолчанию (без --no-html) пишет
                                        # checkponts/best_<step>_report.html:
                                        # все секции + FULL OUTPUT (полный вывод)
```

Флаги: `--moves` (Adam-ценз: кто учится/застрял), `--carry` (B18: влияние
стримингового состояния на хвост), `--depthgrad` (пер-слой CE-градиенты, M31),
`--no-html/--no-live/--no-gradinfo/--quick`, `--log` (парсер лога → HTML-дашборд).
Windows: пайп-вывод кодируется в CP консоли (GetConsoleOutputCP) — без крокозябр.

---

## 25. Состояние (свежий прогон)

Сентябрь 2026. Прогон с нуля: `FORCE_FRESH=True`, seq 384, batch 1, 150k шагов,
A100 40GB. На шаге 10450 — **лучший val 8.2126** (глубина 24/24, 12 улучшений
из 21 eval, 0 NaN/Inf, 0 застрявших групп из 1860).

**VAL-траектория:** 10.83@250 → 10.45@1000 → 9.88@3135 → 9.26@5225 → 9.04@6270
→ 8.47@8360 → 8.34@9405 → **8.21@10450** (спад поздней фазы ≈ −0.13/1000 шагов).
Против старого прогона (best 8.49@7315): старт быстрее, финал лучше. До
униграммы корпуса (7.48 нат) — 0.73 ната. Тренировочный CE к 10450 — 4.2–4.6
(вплотную к биграмме 4.21).

**Глубина:** 8→12→16→20→24 (4180/7315/9405/10450). Все 4 срабатывания
плато-детектора — на отравленной σ (val-спайки 11.57/66.37 раздули sigma;
два последних сработали на улучшениях). Каждое открытие следовало за
улучшением val, но триггер требует спайк-робастности (M65).

**Спайки — два класса, оба самовосстанавливаются (best.pt цел):**
* *CE-путь (голова):* head_wall 89–1772, sat=1, g_log_temp 514–1445, ce 22–430
  (330, 550, 2915, 6930, 7810, 9900, 10285, 10890, 11275, 11825, 12155);
* *Банк-путь (на полной глубине):* **11660: diversity=53 660 при ce=4.69**;
  **11770: loss=13.4M при ce=4.48** — g_fusion2=1.17e9, g_wk/wv/wq=1.3e4–3.1e4,
  g_readout=3.5e4. Взрыв aux-терма, CE-путь чистый. Kill-switch (measure-only)
  не поймал — он ничего не обрезает.

**Четыре «спящих» канала** (инструментация их видит — это и есть пост-аудит
паттерн «построено, но не подключено»):

1. **CARRY=0.0001 (регресс B18)** — стриминговое состояние не влияет на хвост
   (на 3135 было 0.05–0.12). Гипотеза «инъекция не во всех слоях» опровергнута
   кодом (`stack.py:568` — inject_layer в каждом слое, масштабируется
   maturity/τ_norm). Диагностика: нормы stream in/out по глубоким слоям,
   величина вклада против транка. Критично: ядро архитектуры под вопросом.
2. **Logit cache** — gate σ(−10), h-entries=0 (ни одной записи при 526
   R1-срабатываниях). Identity by design; эксперимент — sampling 0.15–0.20.
3. **UCL** — чтение закрыто (read_scale σ(w)=0.02; пол M64.1 держал до 5000),
   запись жива (births=3, conf=0.60, 7/8 слотов). M65: валюта/UCB.
4. **Reasoning** — `head_srl_apply=False` (M53d, осознанно): refine разрушителен
   (постмортем 1045), по умолчанию только телеметрия; A/B natural>FULL — то же
   подтверждение.

**Мост:** bridge_conn ≈ ln(383)=5.95 весь прогон — контрастив не учится;
intent-поток раздут (mean 5.6e4, max 1.4e5), salience к 10450 равномерна.
**Фантом-банк — цикл отработан впервые** (рождение 7590 → взрыв 8030–8140 →
консолидация 9515: 17 в архиве; итог 18 births/23 obs/102 merged/120 dropped).
Рождения при lacuna_rel<1 — калибровка порога в M65.

**WAKE-детектор (10450):** 3 слоя с gate>0.75 (L3=0.94, L2=0.85, L8=0.82),
W_std-маркеры [WAKE] — MLP-пробуждение расползается; maturation L4–L8=0.92 /
L0–L3=0.18 / хвост растёт (рампа до ~12000); profile — **middle-first**.
Depthgrad перевернулся: L0/Llast=0.08 (было 16.3), L23=702.

**M65 — очередь** (протокол и pre-registered A/B — docs/WHITEBOARD.md):
(1) диагностика CARRY-регресса; (2) реальный AuxKillSwitch (порог по
diversity/g_fusion2, бюджет max_off); (3) кап bal_sc 10→3–4; (4) спайк-робастность
плато-детектора; (5) валюта/UCB для UCL-чтения и моста; (6) шина доказательств,
predictive memory, per-bit emphasis; (7) нормализация reasoning-пути;
(8) калибровка триггера фантома; (9) причинность val-спайков (eval на свежем
окне против «горячего» тренировочного стриминга).

---

## Эпилог

EVA-CLM — ставка на то, что язык моделируется **без квадратичного внимания,
без словарных матриц и без десятков подобранных констант**: суперпозицией
— хранить, τ-полем — выводить временем, кодом — говорить. Архитектура
построена так, чтобы её можно было опровергнуть. Это самое ценное, что в неё
вложено.

**Лицензия:** экспериментально. API меняется, миграция чекпоинтов явная и
поимённая. Опровержения и kill-tests приветствуются больше аплодисментов.
